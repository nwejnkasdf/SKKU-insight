# 결정 백로그 (Decision Backlog)

본 문서는 docs/ 전반에 흩어진 모든 `<!-- TODO: -->` / `# TODO:` 마커를 한 곳에 모아 우선순위(P0/P1/P2)를 부여하고, P0 외 항목은 reasonable default 또는 stub 전략을 제시한다. 후속 에이전트가 자기 모듈을 구현할 때 본 표를 따르면 막힘 없이 진행할 수 있다.

분류 기준:
- **P0 — 구현 시작 전 필수 결정**: 사용자 외부 자원·정책에 의존하므로 default가 불가능. 미해결 시 해당 에이전트가 막힘.
- **P1 — Default로 진행 가능**: 합리적 default를 코드/config에 박아 두고, 시연 후 튜닝.
- **P2 — 폴리시·검증 단계 항목**: 1차 구현 동안은 stub 또는 후순위. 본 시연에 영향 없음.

원본 마커 위치는 `file:line` 형태로 기록.

---

## P0 — 구현 시작 전 필수 결정 (0건, 모두 해소됨)

### P0-1. DoRA 낚시성 모듈 경로 공유 — **해결됨 (2026-05-11)**
- **원본**: `algorithms/clickbait-integration.md:9`, `sdd/architecture.md:74` (해소되어 마커 제거됨)
- **결정**: 모듈 위치 = `clickbait_module/`. 가중치 호스팅 = HuggingFace Hub private repo. 서빙 엔진 = **vLLM** (DoRA를 base에 사전 머지 후 일반 base로 로드 + continuous batching). 호스팅·transport는 운영 시점 결정 — backend는 `CLICKBAIT_SERVICE_URL` env로만 호출하며 transport·호스팅과 무관.
- **코드 phase 산출**: `clickbait_module/app/` (FastAPI + vLLM AsyncLLMEngine), `clickbait_module/scripts/merge_adapter.py` (peft `merge_and_unload()`). P1-8·P2-7도 본 결정으로 자연 해소(아래 참조).

## C-급 (이전 분석에서 식별, 인터뷰로 해소된 항목)

| ID | 원래 제기된 문제 | 해소 위치 |
|---|---|---|
| C-1. UserBroadInterest 매핑 누락 | onboarding 선택은 14 active day 한정 prior boost로 동작, 영구 저장 매핑 테이블 불필요로 결정 (행동이 root 모델). 영구 보존 필요 시 `algorithms/cso-topic-traversal.md §1.2` 참조 | `algorithms/cso-topic-traversal.md §1.2`, `decisions.md §4` |
| C-3. Cold-start pseudo Document FK 정합성 | 1차 시연에서는 Source 테이블에 sentinel 행 (`name="cold_start_pseudo"`) 시드 + content_type="pseudo_cold_start" 별도 처리. P1로 격하해 default 진행 | `algorithms/cold-start.md` (TODO 마커 유지) |
| C-4. emerging leaf 노출 경로 부재 | traversal trace 모델 도입으로 자연 해소: emerging은 active trace path 끝 산하에서만 분기되어 current 카테고리 = core 슬롯에 자연 포함. core 슬롯 5개 중 1개는 emerging quota | `algorithms/cso-topic-traversal.md §6.2`, `algorithms/recommendation-ranking.md` Core 섹션 |
| C-5. 사용자 × CSO 토픽 상태 머신·전이 룰 (인터뷰에서 신규 식별) | `UserCSOTraversal` trace 객체 도입 + active day 기반 라이프사이클로 해소. SRS Open Issue 5로 등록 | `algorithms/cso-topic-traversal.md` 전체, `decisions.md §4` |

> **C-2 (NFR-21 30일 grace period)** — A2 (2026-05-11) **부분 해소**: 1차 시연은 RQ async + worker function이 5분 내 CASCADE 실행으로 변경 (즉시 inline cascade가 아닌 async). NFR-21의 30일 grace는 여전히 post-시연 폴리시 단계. async worker 자체가 향후 grace period 도입 시 동일 함수 재활용 경로를 제공. 운영 시 NFR-21 완전 정합 주의. 자세히는 [`api/consent.md`](api/consent.md) 비즈니스 룰.

### C-급 신규 해결 (A2 본문 세션, 2026-05-11)

| ID | 원래 제기된 문제 | 해소 위치 |
|---|---|---|
| C-6. Refresh replay 감지 시 user_id 역추적 불가 (opaque token) | HMAC index `:rotated` 마커 패턴으로 해결: rotation 시 `refresh_index:{HMAC(old)}` 키를 삭제하지 않고 값을 `{user_id}:{jti}:rotated`로 OVERWRITE (TTL 유지). 재사용 감지 시 user_id 추출 → `revoke_all_user_refresh()` family revoke. | [`security/token-handling.md`](security/token-handling.md) §회전, [`security/auth-flow.md`](security/auth-flow.md) §3 |
| C-7. User.email 대소문자 변형 우회 가능 | 3겹 정규화 방어: Pydantic validator + service 계층 + DB functional index `LOWER(email)` partial UNIQUE. | [`data/schema.md`](data/schema.md) User, [`security/auth-flow.md`](security/auth-flow.md) §결정 핀, [`api/auth.md`](api/auth.md) 비즈니스 룰 |
| C-8. SourcePolicy 3행 시드 trust_level 미명시 | (academic, high), (vendor_blog, high), (tech_news, medium), 모두 `collection_rule={}` + `enabled=true`. A2 마이그레이션에 op.bulk_insert. | [`data/schema.md`](data/schema.md) §시드 |
| C-9. /admin/users 마스킹 1글자 local part 처리 | 길이≥2: 첫·마지막 글자 유지 + `***`. 길이=1: 전체 마스킹 fallback `***@domain`. | [`api/admin.md`](api/admin.md) 비즈니스 룰 |
| C-10. User.last_login_at 컬럼이 schema.md User 정의에 누락 (auth-flow.md / api/auth.md 는 갱신 명시) | `last_login_at: DateTime(timezone=True), nullable=True` 컬럼 추가. auth/service.login 이 UPDATE. A2 자체 검수에서 발견. | [`data/schema.md`](data/schema.md) User |
| C-11. passlib + bcrypt 4.x 호환성 깨짐 | passlib 1.7.4 가 bcrypt 4.x 의 `__about__` 제거·72byte ValueError 처리 못 함. `passlib` 의존 제거 + `bcrypt` 직접 호출 + SHA-256 hex pre-hash 패턴 (64 ASCII bytes — 72 한도 + null 회피). UTF-8 한국어 128자 정책 보장. A2 자체 검수에서 발견. | [`security/password-policy.md`](security/password-policy.md) §저장 |
| C-12. UserCSOTraversal `array_length(path,1)>=1` CHECK 가 빈 배열 우회 | PostgreSQL `array_length('{}'::uuid[],1)=NULL` 이고 CHECK NULL 통과 → `{}` 저장 가능. `cardinality(path)>=1` 으로 교체 (빈 배열 0 반환). codex review (A2 self-review 후) 에서 발견. | [`data/schema.md`](data/schema.md) UserCSOTraversal, 0001 migration |
| C-13. logout 시 refresh token 폐기 누락 | `/auth/logout`·`/admin/auth/logout` 이 access jti 만 denylist, refresh 메타는 active 유지 → 탈취 refresh 로 새 access 발급 가능. `LogoutRequest.refresh_token` body 도입 + `revoke_refresh_by_token()` 신규(`:revoked` 마커). codex review C-2. | [`api/auth.md`](api/auth.md) §비즈니스 룰, [`api/admin.md`](api/admin.md) `AdminLogoutRequest`, [`security/token-handling.md`](security/token-handling.md) |
| C-14. `must_change_password=true` admin 의 다른 API 차단 부재 | `get_current_admin` 이 must_change_password 상태도 통과시켜 `/admin/users` 호출 가능. admin-bootstrap.md 와 명세 불일치. 가드: change-password/logout 외 경로는 409 `admin.must_change_password`. codex review C-4. | [`api/admin.md`](api/admin.md) §비즈니스 룰, `app/security/deps.py:get_current_admin` |
| C-15. refresh rotation race (read-then-write) | 동일 token 동시 2 refresh 요청 → 둘 다 active pointer 인식 → 양쪽 모두 새 token 발급 → family revoke 무력화. Redis Lua 스크립트로 GET+상태 확인+`:rotated` 전이+meta active='0' 셋팅을 atomic CAS. codex review C-7 (P2 였으나 사용자 결정으로 P0 처리). | [`security/token-handling.md`](security/token-handling.md), `app/security/jwt.py:_LUA_VERIFY_AND_MARK_ROTATED` |
| C-16. account_deletion worker 가 `refresh_index:*` 전역 SCAN/DEL | 한 사용자 삭제 시 모든 사용자 refresh_index 삭제 → 다른 사용자 강제 로그아웃. SCAN 패턴 제거 + 본 사용자 namespace 만 정리(`refresh:{user_id}:*` + `idemp:onboarding:{user_id}:*`). refresh_index 는 TTL 14d 만료에 위임. codex review C-1. | `app/worker/jobs/account_deletion.py` |
| C-17. admin pagination Python tuple 비교 런타임 에러 | `(User.created_at, User.user_id) < (ts, uid)` 가 Python tuple comparison 으로 평가돼 SQLAlchemy 표현식 boolean 평가 → TypeError → 2페이지 cursor 호출 실패. `tuple_(...)` SQL helper 사용으로 row-comparison SQL 생성. codex review C-6. | `app/admin/users_service.py:list_users` |
| C-18. `/admin/auth/refresh` JwtAuthMiddleware 화이트리스트 누락 | refresh endpoint 가 인증 필요로 처리돼 access 만료 후 갱신 불가. `/auth/refresh` 와 동일하게 우회. codex review C-3. | `app/middleware/jwt_auth.py:WHITELIST_PATTERNS` |
| C-19. LLM 동시성 가드 (`_global_sem`, `_user_sems`) 가 per-process asyncio.Semaphore | `UVICORN_WORKERS>1` 또는 다중 컨테이너에서 전역 `LLM_MAX_CONCURRENT=8` 약속 깨짐 (워커 4 × 8 = 실효 32). Redis 분산 semaphore (Lua atomic acquire/release + INCR/DECR + 60s TTL) 로 교체. `RedisKey.llm_global_active_count()` / `llm_user_active_count(user_id)` 신규. `LLM_SEMAPHORE_ACQUIRE_TIMEOUT_SECONDS=30` env 추가. multi-worker 동시성 정책 검수에서 발견. | [`sdd/concurrency.md §5`](sdd/concurrency.md), `app/llm_provider/_concurrency.py`, `app/contracts.py` RedisKey |
| C-20. multi-worker 정책 부재 | `UVICORN_WORKERS` env 신규 (default 1). DB pool 은 process 독립이므로 운영자가 `UVICORN_WORKERS * PG_API_POOL_MAX + PG_WORKER_POOL_MAX ≤ PostgreSQL max_connections` 가 되도록 조정. docker-compose.yml api command 가 `--workers ${UVICORN_WORKERS:-1}` 사용. env-vars.md §Worker 병렬 정책 에 공식 표 추가. | [`ops/env-vars.md`](ops/env-vars.md) §Worker 병렬 정책, `docker-compose.yml`, `config.py` |
| C-21. C-15 (rotation Lua CAS) 의 잔여 race window | C-15 fix 는 `verify+mark rotated` 만 Lua, `issue_refresh` (new meta HSET + new index SET) 가 Python 별도 호출이라 T1 'ok' 후 issue 진행 중 T2 'replay' family revoke 가 T1 신규 meta 를 SCAN 누락 가능한 좁은 window 존재. **옵션 B 채택** — `_LUA_VERIFY_ROTATE_ISSUE` 단일 Lua 가 verify · rotate · new meta INSERT · new index SET 4 변경을 단일 unit 으로 처리해 window 완전 제거. Python 은 `new_token = secrets.token_urlsafe(64)` + `new_jti = uuid4()` + `new_hmac = HMAC(new_token)` 만 사전 생성. 사용자 결정 2026-05-11. | [`security/token-handling.md`](security/token-handling.md) §동시 rotation race 차단, `app/security/jwt.py:_LUA_VERIFY_ROTATE_ISSUE` |
| C-22. JWT_SECRET placeholder 통과 (lifespan validator 미흡) | `.env.example` 의 `change-this-to-64-char-...` (76자) 가 길이만 검사하는 lifespan 통과 → 운영자가 .env 복사만 하고 시크릿 미교체 시 공개 HS256 키로 토큰 위조 가능. lifespan 에 known-bad 리스트 (`_JWT_SECRET_PLACEHOLDERS`) + prefix 패턴 (`change-this-to-*` 등) 차단. POSTGRES_PASSWORD 도 동일 룰 적용. codex v2 review. | `app/lifespan.py:_validate_secrets` |
| C-23. 계정 삭제 진행 중 access 토큰 살아있음 | `/consent/account-deletion` 가 202 반환 후 worker 가 DB row 삭제 전까지 access TTL (≤15m) 동안 유효 → 삭제 진행 중 personalization API 호출 가능. `RedisKey.account_deletion_pending(user_id)` 신규 SOR + `JwtAuthMiddleware` 가 user audience 토큰에 대해 본 lock 검사 → 존재 시 401 `consent.deletion_in_progress`. worker 완료 시 lock 명시 DEL. codex v2 review. | `app/contracts.py` RedisKey, `app/middleware/jwt_auth.py`, `app/consent/service.py`, `app/worker/jobs/account_deletion.py` |
| C-24. onboarding lock 조기 해제 | `post_interests` finally 가 lock 즉시 삭제 → cold-start job 큐잉 중 같은 사용자 더블클릭 시 중복 enqueue. 본 패치는 finally 제거 + TTL 30s 자연 만료에 위임 (lock 살아있는 동안 다음 POST 는 진행 중 request_id 회귀). cold-start orchestrator (A8) 가 완료 시 명시 DEL 가능. codex v2 review. | `app/onboarding/service.py:post_interests` |
| C-25. CORS 미들웨어 등록 순서 오류 | Starlette 은 마지막 add 가 가장 바깥 → 기존 CORS 먼저 add 가 가장 안쪽 → JwtAuth 401 반환 시 CORS 헤더 미적용 → 브라우저/Electron 이 cross-origin 으로 ErrorResponse 못 읽음. RequestId → JwtAuth → ConsentGate → **CORS (마지막)** 순서로 재정렬. codex v2 review. | `app/main.py:create_app` |
| C-26. JSONB 시드 type 깨짐 | alembic `op.bulk_insert` 에 `"collection_rule": "{}"` (Python str) 전달 → JSONB bind processor 가 `'"{}"'` JSON 문자열로 직렬화 → 코드 `policy.collection_rule.get(...)` AttributeError. `{}` dict 로 변경. `source.extra` 도 동일. codex v2 review. | `backend/alembic/versions/0001_initial_a2_tables.py` SourcePolicy + Source seed |
| C-27. conftest 의 alembic `command.upgrade` 가 nested asyncio.run | pytest async fixture 안에서 `command.upgrade` → env.py 가 `asyncio.run(...)` → 이미 running loop → `RuntimeError`. 모든 integration test 부트 실패. `subprocess.run([sys.executable, "-m", "alembic", "upgrade", "head"], ...)` 로 분리해 자체 이벤트 루프에서 실행. codex v2 review. | `backend/tests/conftest.py:db_engine` |
| C-28. `ASGITransport(lifespan=...)` TypeError | httpx ≥ 0.27 의 `ASGITransport` 가 `lifespan` 인자 미지원 (lifespan 처리를 external manager 로 분리) → client fixture 생성 시 `TypeError`. 인자 제거. 본 conftest 는 fixture 가 직접 DB/Redis init/dispose 하므로 lifespan 자체 미실행. codex v2 review. | `backend/tests/conftest.py:client` |
| C-29. Makefile `pytest backend/tests` 경로 오류 | Dockerfile `WORKDIR /app` + `COPY . /app/` (backend context) → 컨테이너 안 path 는 `/app/tests`. `make test` 가 `backend/tests` 경로로 실행 시 collection 실패. `pytest tests -v` 로 수정. codex v2 review. | `Makefile:test` |
| C-30. ConsentGate 가 JwtAuth 보다 먼저 실행 — privacy bypass | codex v2 #4 fix 에서 middleware add 순서를 `RequestId → JwtAuth → ConsentGate → CORS` 로 했으나 Starlette 은 마지막 add 가 가장 바깥 → 요청 흐름 `CORS → ConsentGate → JwtAuth → RequestId → endpoint`. ConsentGate 가 JwtAuth 보다 *먼저* 실행 → `request.state.user_id`/`aud` 미셋팅 → "user audience 만 검사" 가드가 false → 모든 protected endpoint 가 consent 검사 없이 통과. 올바른 add 순서: **ConsentGate(첫) → JwtAuth → RequestId → CORS(마지막)** → 요청 흐름 `CORS → RequestId → JwtAuth → ConsentGate → endpoint`. codex v3 review P1. | `app/main.py:create_app` |
| C-31. ruff `F401` (미사용 import) + `RUF002` (× MULTIPLICATION SIGN) + `E402` | `make lint` (`ruff check app/ scripts/`) 가 미사용 import (admin/auth_service.UUID, lifespan.RedisDB, consent_gate.AsyncSession, onboarding/service.json, scripts/check_api_docs.Path, scripts/check_contracts.Path) 와 한글 docstring 의 `×` 곱셈 기호, `setup()` 후 import (E402) 로 실패. `--fix` 자동 제거 + `×→x` + `# noqa: E402` 추가. codex v3 review P2. | `app/`, `scripts/` 전반 |
| C-32. mypy `--strict` 26건 위반 — `make lint` 깨짐 | codex v3 P2 #3 의 `dict|None` 외에도 다양한 strict 위반: ① Mapped[dict] bare generic (source.py, source_policy.py — `dict[str, Any]` 로 명시) ② `await redis.X()` 의 redis.asyncio dual sync/async sig (jwt.py 3건, onboarding/service.py 3건, lifespan.py 1건 — `# type: ignore[misc]`) ③ `getattr(settings, reg["cron_attr"])` reg dict 타입 추론 실패 (scheduler.py — `TypedDict _JobRegistration`) ④ `tuple_()` 인자 SQLAlchemy 표현식 강제 (users_service.py — `or_()`/`and_()` 명시 분기) ⑤ `base64.binascii` AttributeError (users_service.py — `import binascii` 직접) ⑥ Redundant cast (deps.py — 제거) ⑦ Unused `# type: ignore` (protocol.py, lifespan.py, onboarding/service.py — 제거) ⑧ AsyncSession annotation 변수 표기 (consent_gate.py — 제거) ⑨ `from rq import Connection` rq 2.x 제거됨 (worker/__main__.py — Queue/Worker 가 connection 인자 직접 받음) ⑩ structlog processor return type (structlog_mask.py — `Mapping[str, Any]`) ⑪ `sys.exit(main() or 0)` 패턴 (scheduler.py, worker/__main__.py — `main()` returns `int`) ⑫ `Returning Any from function declared to return bool` (jwt.py is_jti_denylisted, consent_cache.py — 명시 bool cast). 추가로 `pyproject.toml mypy override` 에 `redis.*` `disable_error_code=["misc"]` 추가. codex v3 P2 발견 후 자체 mypy 실행으로 26건 확장. | `backend/pyproject.toml` mypy override, 12 app/* 파일 |
| C-33. v13 라운드 A4 Topic-driven Pivot (2026-05-11) | 프로젝트 원안("에이전트 기반 추천 시스템 하네스 + ChatGPT 같은 검색 활용 + 토픽이 먼저고 문서가 나중") 회복. 6 source 어댑터(arxiv/openalex/s2/dblp/RSS/네이버BS4) push-from-sources 모델 → LLM tool-use(web search) topic-driven pull 모델. Source 테이블 = sentinel 1행 `llm_search` + publisher Document.extra JSONB. Clickbait_module = default 비활성 (사용자 News 소스 명시 활성화 시만). NFR-25 정합 = LLM self-summary instruction. P1-6/P2-3/P2-4 무효 마킹. SRS FR-22~25 식별자 보존 + 명세 v13 라운드 해석. 사용자 결정 12건 (LLM provider toggle / query 구성 LLM 자율 / CollectionJob 단위 / Trigger / Jitter / Dedup / Document.PK / 실패 정책 / history pagination / topic/documents 본문 cross-cutting / Source 테이블 sentinel / clickbait 비활성). | `decisions.md §5·§8·§10`, `decision-backlog.md` (본 항목), `srs/02·03`, `algorithms/cso-mapping·clickbait-integration`, `data/sources-registry·schema`, `sdd/architecture`, `api/collection`, `prompts/03-A4-collection`, `AGENTS.md`, `README.md`, `docs/README.md`, `prompts/README.md` |
| C-34. v13 round 2 — A4 코드 + Codex 감사 fix (2026-05-16) | round 1 본문 구현 후 Codex 감사가 Critical 3 / Suggested 9 / Nit 3 = 15건 식별. 사용자 결정: contracts PR-1 (JobType enum) + ORM index 미러링 PR-2 + 본문 fix PR-3 3단 분할. **C-01** OpenAI Responses API web_search 정식 구현 (GPT-5.5, reasoning 파라미터 OpenAI default 위임). **C-02** dedup 2 그룹 분리 + DocumentTopic upsert (토픽 엣지 누락 차단). **C-03** pg_insert.on_conflict_do_nothing 로 cross-user URL/DOI 충돌 흡수. **S-01/02/03** run-now 가 queued row 먼저 INSERT → job_id 정합 + enqueue lock (TTL 10s) + RQ retry re-raise. **S-04** documents cursor coalesce(published_at, created_at). **S-05** CollectionJobMeResponse next_cursor/has_more. **S-06** JobType inline → contracts.JobType enum. **S-07** ORM 에 partial UNIQUE index 5종 미러링. **S-08** lifespan `_validate_llm_provider` 가드 ({mock, openai} 외 차단). **S-09** content_type 동적 분류. **N-02** topic/router docstring. **N-03** prompt version hash → fixture invalidate. 사용자 시연 발화: "Claude" 전면 제거 → "GPT-5.5". | `backend/app/{contracts, lifespan, config/__init__, collection/*, topic/{documents_service, router}, llm_provider/{openai, mock}, db/models/{document, document_topic}, worker/jobs/collection}.py`, `alembic/versions/0003_*`, `tests/collection/{test_dedup, test_audit_regressions}.py`, `tests/llm_provider/{test_openai_search, test_lifespan_provider_guard}.py`, `decisions.md §10`, `AGENTS.md`, `.env.example` x2 |
| C-35. v13 round 3 — Codex 재감사 fix (2026-05-16) | round 2 fix 검증 위해 Codex 재감사 → 13건 RESOLVED + 3건 PARTIAL (C-01·C-03·S-03) + 신규 7건 (R2-C01·C02 Critical + R2-S01·S02·S03·S04·S06 Suggested + R2-N01 Nit). 본 round 3 모두 fix. **R2-C01·C02** `_insert_document_idempotent` 가 partial unique index 에 대한 `on_conflict_do_nothing(index_elements=...)` infer 실패 → untargeted `on_conflict_do_nothing()` + canonical→doi pre-lookup 패턴으로 재작성. canonical_url + doi 둘 다 있을 때도 모든 unique constraint 안전 처리. **R2-S01** trigger_run_now 가 enqueue 실패 시 queued row 를 FAILED 마킹 + 503 응답. **R2-S02** orchestrator 가 existing_job_id 재사용 시 retry_count++ + finished_at/failure_reason 초기화 (RQ retry 시 정확 추적). **R2-S03** openai.py 가 `response.json()` ValueError 도 ProviderError 로 wrap (C-01 PARTIAL 해소). **R2-S04** `_upsert_document_topic` 이 `DO UPDATE SET confidence = greatest(excluded, current)` — 더 높은 confidence 유지 (staleness 차단). **R2-S06** `_insert_document_idempotent` 가 `(UUID|None, bool)` tuple 반환 → is_new 만 inserted_count 증가 (통계 정확). **R2-N01** hash_prompt_search 가 SYSTEM_PROMPT_VERSION 외에 SYSTEM_PROMPT_TEMPLATE 본문 SHA256 도 포함 → 본문 1자 변경 시 수동 bump 없이도 fixture 자동 invalidate. R2-S05 (함수형 정렬 인덱스) 는 운영 단계 후속. | `backend/app/{collection/{orchestrator, service}, llm_provider/{openai, mock}}.py`, `tests/collection/test_audit_regressions.py` (TestRound3CodexFixGuards 7 신규), `AGENTS.md`, `decision-backlog.md` (본 항목) |
| C-37. A6 본문 구현 + Codex round 1 감사 fix (2026-05-17) | A6 (interest-bayesian) Phase 1 본문 + 1차 Codex GPT-5.5 xhigh 감사. **본문**: alembic 0004 (UserEvent/UserInterestState/SavedDocument/HiddenDocument/NotInterestedTopic/SystemConfig 6 신규 + 12 partial UNIQUE + system_config 2 row seed) + app/interest/ 9 파일 (bucket/config_loader/decay/idempotency/propagation/router/schemas/service/topic_distribution) + app/events/ 2 파일 (buffer 5초 batch flush + active_day atomic counter) + 9 endpoint + onboarding bootstrap 협업 + lifespan EventBuffer task + worker/jobs/interest_decay 본문 (daily cron 18 UTC = 03 KST). **결정 매트릭스 17건** (사용자 결정 12건 + 후속 결정 5건): Decay daily cron only / Redis dwell cap Lua atomic / 14-day boost 만료 daily 차감 (boost_applied_at_active_day 컬럼) / cluster + 1-hop child boost (predecessor) / propagation feature flag env (INTEREST_PROPAGATION_ENABLED default false) / payload-hash idempotency 200/409 / not-interested 하이브리드 정렬 2 (Bayesian P1-4 분배 + NotInterestedTopic 최고 confidence 1건 INSERT) / cache invalidate recommendation:{user_id} 단일 / active_day 갱신 5 endpoint / system_config A6 read-only + A10 admin UI 분담 / 207 Multi-Status batch / max-50 + bucket-sorted /interest/state. **Codex 1차 감사 8건 fix**: **C-01** atomic UPSERT lost update (UPDATE→INSERT DO NOTHING 2-step 의 동시 INSERT race 시 한쪽 손실) → INSERT ON CONFLICT DO UPDATE 단일 SQL 로 교체, partial UNIQUE 3종 별 `ON CONFLICT (cols) WHERE pred` 명시. **C-02** idempotency cache-before-commit (store_idempotent 가 db.commit() 전 호출 → commit 실패 시 cache 만 잔존) → IngestResult 에 payload_hash + client_request_id 필드 추가, router 가 db.commit() 후 `_store_idempotent_after_commit()` 호출. **S-01** IntegrityError race try/except. **S-02** dwell tick cap Lua atomic INCR+EXPIRE (`_DWELL_INCR_LUA`). **S-03** decay GREATEST(:alpha_prior, computed) floor — child row (+0.5 boost) alpha 음수 차단. **S-04** EventBuffer.add 가 lock 안 `_stopped` 검사 + True 시 즉시 callback fallback. **S-05** Settings.SYSTEM_CONFIG_REQUIRED bool toggle (default true → fail-fast). **S-06** onboarding bootstrap `db.begin_nested()` savepoint 격리. | `backend/app/contracts, config/__init__, db/models/{6신규}, alembic/versions/0004, events/*, interest/*, lifespan, onboarding/service, worker/jobs/interest_decay`, `tests/interest/* (13 신규)`, `docs/sdd/contracts, ops/env-vars, api/interest, data/schema`, `.env.example x2`, [PR #18](https://github.com/nwejnkasdf/SKKU-insight/pull/18) commit 9890a17/faac655/4c3f8f1/8ebeeca/9e6242b |
| C-38. A6 Codex round 2 재감사 fix + 시연 검증 (2026-05-17) | round 1 fix 검증 위해 Codex 재감사 → round 1 fix 8건 중 6건 FULLY RESOLVED, S-01 PARTIAL (C-03 의 원인). **신규 4건 fix**: **C-03 batch race regression** (round 1 S-01 의 db.rollback() 가 batch 안 앞선 entry 의 user_event row 소실 + Redis SETEX 만 잔존 → false positive cache) → `_record_user_event` 가 `pg_insert(UserEvent).on_conflict_do_nothing(...).returning(UserEvent.event_id)` 패턴. race 시 None 반환 → caller 가 check_idempotent lookup → duplicate 분기, 트랜잭션 보존. **S-07** IntegrityError 오분류 (round 1 의 except 가 FK 위반도 duplicate 처리) → C-03 fix 의 ON CONFLICT DO NOTHING 패턴이 자연 해소, IntegrityError import 제거. **S-08** test_audit_regressions.py test_c01 가 redis=None 전달 → redis_client fixture 주입. **N-01** decay boost_expired metric 과대 집계 (boost 미적용 row 포함) → `last_event_active_day IS NOT NULL` 추가 필터 + 보수적 근사 주석. **회귀 테스트 1건** (test_c03_batch_race_preserves_prior_inserts). **통합 시연 검증** (docker compose 격리 환경, host port 5434/6380/8001 override): alembic 0001→0004 적용 + 22 tables (A6 신규 6 모두) + system_config_loaded=true + signup/consent/JWT 흐름 + /interest/state 200 + NFR-04 마스킹 (score 키 미노출) + POST /events view 200 + idempotency 200(match)/409(mismatch) + /events/batch 207 (3 accepted + 1 duplicate) + C-03 batch race fix 정합 (user_event 4 row 정확 보존). | `backend/app/interest/{decay, service}, tests/interest/test_audit_regressions.py`, [PR #18](https://github.com/nwejnkasdf/SKKU-insight/pull/18) commit 5d2feec |
| C-39. A7 본문 + Codex round 1/2 fix (2026-05-17) | A7 (leaf-lifecycle + traversal) Phase 2 본문 — trace operation 4 → 5 (merge 신규) + leaf lifecycle D 하이브리드 + Strict 검증 + INTEREST_PROPAGATION_ENABLED=true 토글. **본문**: app/traversal/ (protocol/default/operations/queries/merge_evaluator 5 파일) + app/leaf_lifecycle/ (protocol/hybrid_d/rule_evaluator/llm_identifier/leaf_merge_evaluator/strict_validation 6 파일) + worker/jobs/ (leaf_lifecycle/trace_merge/daily_lifecycle_evaluation 3 신규 + merge_evaluation 본문 교체) + scheduler 3 신규 cron + alembic 0005 (UserCSOTraversal.merged_into_trace_id + ck_collection_job_type 갱신) + topic_lifecycle.toml 신규 + contracts (JobType.TRACE_MERGE + ErrorCode 5 + RedisKey 3 + Settings 33+). 4 commit: 7332b34 PR-1 contracts SOR + 9f24ac3 PR-2 alembic + 65f415f PR-3 본문 + 37b686f PR-3 tests 47 unit. **결정 매트릭스 23건** (decisions.md §12, 사용자 결정 17 + 후속 6): PR-stack 6-commit / Codex 3 라운드 / 실 GPT-5.5 시연 / propagation 토글 / 하이브리드 강등 / Cap 폐지 / alembic 0005 / Strict 검증 / trace_anchor retry cap=1 / merged leaf 추천 제외 / trace merge operation 신규 / input D union / split T 단축+T'=분기점+B / merge winner=max activity / daily 18 UTC LLM. **Codex 1차 감사 10건 fix** (commit 485da9b): **C-01** execute_retract status='stale' + cardinality > 1 + RETURNING rowcount 확인 (path UPDATE 실패 시 leaf 변경 skip). **C-02** evaluate_retract leaves_to_remap = retracted_cso 매핑 leaf 한정 + new_path 끝 노드 remap fallback. **C-03** leaf_lifecycle._collect_input_documents A branch 완전성 (leaf 매핑 + cso_topic IN trace.path AND leaf_topic_id IS NULL 양쪽 + 24h cutoff). **S-1** daily_lifecycle archive_threshold = stale_idle + archive_after_stale. **S-2** strict_validation anchor = path + get_children 1-hop (find_descendants transitive 회피). **S-3** daily_lifecycle user-mutex (traversal_lock). **S-4** leaf_merge execute_merges status='active' filter + RETURNING. **S-6** recommendation_cache invalidation 4 worker job. **S-7** docs/decisions.md §12 신규. **N-1** scheduler docstring "6 cron". **Codex 2차 재감사 5건 fix** (본 commit): **R2-NEW-1 Critical** evaluate_split child_A 처리 누락 — truncated_path=[*path, child_A] + new_path=[*path, child_B] 양쪽 모두 child 포함 + leaf dispatch target=source_child. **R2-RG-1 Critical** trace_merge_job 의 lock key 통일 (trace_merge_lock → traversal_lock) — daily_lifecycle 와 같은 user-mutex 공유. **R2-RG-2 Critical** path 길이 1 시 retract 대신 archive 직접 count + cache invalidate. **R2-RG-3 Suggested** lock release Lua atomic GET+DEL CAS (uuid4 토큰) — TTL 만료 후 다른 worker lock 잘못 해제 차단. **R2-RG-4 Suggested** retract remap composite PK 충돌 회피 — 이미 new_cso 매핑된 leaf 는 retracted 행만 DELETE. **R2-DEF-S5 처리 (Critical 권고)** A6 ingest_event_atomic 끝에 mark_stale_if_idle hook 추가 — score_tail ≤ 0.30 AND idle ≥ 21 active days 인 active trace 즉시 stale 전이 (no LLM, atomic SQL 1회). cold-start trace 생성 hook (TraversalEngine.create_new_trace) 은 A8 진입 시 본문 완성 (plan TBD). **Codex 3차 재재감사 7건 fix**: **R3-RG-C1 Critical** mark_stale_if_idle hook 이 traversal_lock 없이 trace status 변경 — interest/service.py 의 hook 에 lock acquire (uuid4 token + Lua atomic CAS release) 추가, lock 미보유 시 skip (자연 자체 복구). **R3-NEW-C1 Critical** LLM semaphore counter TTL 이 60s hardcode → LLM_REQUEST_TIMEOUT_SECONDS(=180s)+30s 동적 계산 (`_counter_ttl_seconds()`), 호출 도중 counter EXPIRE 차단. **R3-NEW-C2 Critical** OpenAI provider 의 httpx.HTTPStatusError / httpx.HTTPError / json ValueError 를 모두 ProviderError 로 wrap — A7 caller 들의 fallback 정상 진입 보장. **R3-RG-S1 Suggested** daily_lifecycle 의 traversal_lock TTL 을 max(TRAVERSAL_USER_LOCK_TTL_SECONDS, TRACE_MERGE_LOCK_TTL_SECONDS) 동적 계산 — LLM 호출 동반 시 lock 자연 만료 방지. **R3-RG-S2 Suggested** scheduler.py + trace_merge.py docstring 의 trace_merge_lock 잔존 표현 → traversal_lock 정합. **R3-NEW-S2 Suggested** execute_archive 의 SELECT-UPDATE 2-step → UPDATE...RETURNING path 단일 atomic SQL. **R3-NEW-S3 Suggested** cso-topic-traversal.md §3.3 split 트리거 표현 — "path 위 어느 노드든" → "trace 의 현재 path 말단(tail) 노드" 정합 (실제 구현 tail-only fork 와 일치). **R3-NEW-S1 (P2 등재)** leaf_merge execute_merges 의 SELECT FOR UPDATE 도입 — 시연 단계 transaction isolation (READ COMMITTED) + status='active' filter + RETURNING 으로 충분, 운영 단계 P2 backlog. | `backend/app/{contracts, config/__init__, traversal/{protocol,default,operations,queries,merge_evaluator}, leaf_lifecycle/{protocol,hybrid_d,rule_evaluator,llm_identifier,leaf_merge_evaluator,strict_validation}, worker/jobs/{leaf_lifecycle,trace_merge,daily_lifecycle_evaluation,merge_evaluation}, scheduler, interest/service, db/models/user_cso_traversal}.py`, `backend/alembic/versions/0005_a7_traversal_merge.py`, `backend/app/config/topic_lifecycle.toml`, `tests/{traversal/test_{merge_evaluator,audit_regressions}, leaf_lifecycle/test_{rule_transitions,strict_validation,anchor_retry}}.py (47 신규)`, `.env.example x2`, `docs/{decisions §12, decision-backlog, sdd/contracts, ops/env-vars, api/topics}`, `scripts/{check_contracts (JobType 활성화), check_redis_keys (A7 prefix 3)}` |
| C-40. A8 본문 + Codex 3 라운드 audit fix + 실 GPT-5.5 통합 시연 검증 (2026-05-17) | A8 (recommendation engine) Phase 2 후반 본문. core/adjacent/discovery 3 슬롯 (5/3/2) + Cold-start LLM orchestrator + reason_short LLM batch + Document 섹션형 LLM 요약 + single-flight cache + 첫 trace 생성 hook (A7 결정 #6 plan TBD 완성). **본문**: `app/recommendation/` 11 파일 (service / engine / candidates / ranking / diversify / fallback / reasons / cold_start / summary_service / config_loader + router wire) + `app/worker/jobs/cold_start` 본문 + `app/interest/service.py` trace creation hook + `app/config/recommendation.toml` + Settings 9 신규 field. **alembic 0006**: Recommendation + RecommendationSlot + DocumentSummaryCache 3 신규 테이블 + daily UNIQUE 인덱스 + 3 CHECK. **7 commit-stack**: c82ce83 PR-1 alembic+ORM (3 model + schema.md drift fix) + 6476037 PR-2 본문 (~3514 line insertion) + ce35d8e PR-3 tests (9 파일 + conftest, 1852 line) + 15883d1 PR-4 R1 self-review fix (TopicChip dedup) + ebdd11f docs drift + 099f837 PR-5 R2 Codex 재감사 fix (Critical #2 UTC 경계 fallback + Suggested #1 Lua atomic + P2-22/23/24 backlog) + ee627a2 PR-6 R3 통합 시연 fix (GPT-5 temperature/max_tokens 분기). **결정 매트릭스 7건 (사용자 결정 4 + 자체 결정 3)**: PR-stack 7-commit + Codex 3 라운드 / Summary 캐시 = 신규 테이블 DocumentSummaryCache / 첫 trace 생성 hook = cold_start orchestrator + ingest_event_atomic click hook 협업 / PUT /onboarding/interests (FR-55) = stub 유지 (A9/A10 분담) / sentinel `cold_start_pseudo` (A2 alembic 0001 시드) + content_type='pseudo_cold_start' 활성화 / emerging quota = core 5 중 1, 부재 시 active 회수 / score 컬럼 = Recommendation DB 영속 (admin 노출), 일반 사용자 응답 schema 부재 (NFR-04). **§11 anti-pattern 5건 사전 방어** (R1 fix 최소화 목표): #1 cache-before-commit (db.commit→redis.setex 순서) / #2 daily UNIQUE race (on_conflict_do_nothing+lookup fallback) / #3 emerging quota race (candidates SQL 단일 fetch + leaf_status 컬럼) / #4 score 노출 (RecommendationCard schema 부재 + 명시 매핑) / #5 lock token race (Lua atomic CAS DEL + uuid token). **Codex 3 라운드 audit 결과**: R1 self-review 1 fix (TopicChip dedup) + R2 Codex 외부 감사 (Critical 2 / Suggested 1 / Discussion 2 / Acknowledged 6) — Critical #2 UTC 경계 fallback + Suggested #1 Lua atomic INCR+EXPIRE 적용 / Critical #1 + Discussion 2건은 P2-22/24 backlog (운영 단계) / Discussion #1 P2-23 R3 검증으로 해소 + R3 통합 시연 발견 결함 1건 fix (GPT-5 temperature/max_tokens 분기, openai.py). **R3 실 GPT-5.5 통합 시연 검증** (docker compose + LLM_PROVIDER=openai): alembic 0001→0006 통과 (P2-23 functional index `((created_at AT TIME ZONE 'UTC')::date)` PostgreSQL 16-alpine 실 검증) → signup → consent → onboarding (AI/Systems/Theory 3 cluster) → cold_start_job 실 OpenAI chat/completions 호출 (HTTP 200, 61s) → dashboard 10 cards (core 5: Learning to Reason with LLMs, Transformers are SSMs, Llama 3.1, NVIDIA Blackwell, IMO AI silver / adjacent 3: Model Context Protocol, Apple Intelligence, AI Scientist / discovery 2: AlphaFold 3, Willow quantum chip) + Korean reason_short 실측 23~31자 (≤80 검증) + NFR-04 응답 'score' 키 0건 + 2회차 cache hit + cold_start=true + source_name="cold_start_pseudo" sentinel 활용 + R2 fix #2 UTC 경계 fallback 활성. | `backend/alembic/versions/0006_a8_recommendation_tables.py`, `backend/app/db/models/{recommendation, recommendation_slot, document_summary_cache, __init__}.py`, `backend/app/recommendation/* 11 파일`, `backend/app/worker/jobs/cold_start.py`, `backend/app/interest/service.py`, `backend/app/llm_provider/openai.py`, `backend/app/config/{__init__, recommendation.toml}`, `.env.example x2`, `docs/{data/schema, ops/env-vars, decisions, decision-backlog}.md`, `AGENTS.md`, `prompts/README.md`, `backend/tests/recommendation/*` |
| C-41. CodexOAuth 본문 + reasoning_effort fix + 시연 default 전환 + service_tier=fast (2026-05-18) | 직전까지 stub 였던 `CodexOAuthProvider` 를 `codex exec --json` subprocess wrap 으로 본문화 + 사용자 원래 결정 (`reasoning_effort` slot 별 분리) 이 코드에 안 박혀 있던 dead intent fix + 시연 default 를 codex_oauth 로 전환. **방식 확정 근거** (직접 fetch): openclaw docs/concepts/oauth (PKCE flow + auth.openai.com endpoint + `~/.codex/auth.json` 경로) + developers.openai.com/codex/cli/features (Web search 섹션 — `--search` flag / `web_search="live"` config) + developers.openai.com/codex/cli/reference (`--json` / `--output-schema` / `--ephemeral` 옵션) + openai-python types (reasoning_effort 가능 값 `none/minimal/low/medium/high/xhigh` + chat/completions top-level vs responses nested 형식 차이) + 사용자 PC `~/.codex/config.toml` 실측 (`model_reasoning_effort = "xhigh"` 사용 중). **사용자 결정 6건**: codex 방식 = subprocess wrap (자체 PKCE X — openclaw 도 primary path 가 native CLI wrap) / 합법성 = OpenAI 공식 허용 path 간주 / 모델 = 모든 slot `gpt-5.5` / reasoning_effort = high/medium (xhigh 미사용, latency + 5h 세션 한도) / 시연 default = `codex_oauth` (`.env.example` 권고, Settings 코드 default 는 `mock` 유지 — CI 안전) / web_search = `cached` default (latency·비용 안정). **자체 결정 8건** (decisions.md §14): sandbox `read-only` / workdir `/tmp/codex-runtime` / `~/.codex` rw mount (refresh 위해) / lifespan binary 검증 fail-fast / `--output-schema` 임시 파일 + `_SEARCH_OUTPUT_SCHEMA` 강제 / JSONL parser `agent_message` text 만 합침 / usage 매핑 `prompt = input_tokens, completion = output + reasoning_output` / 글로벌 단일 토글 (slot 별 분기 X). **본문**: `app/llm_provider/codex_oauth.py` stub→본문 ~500줄 (complete + search_with_tools + JSONL parser + `--output-schema` 임시 파일 + 6 AntiJoin) + `app/llm_provider/__init__.py` docstring + `app/config/__init__.py` Settings 6 신규 (`LLM_REASONING_EFFORT_HIGH/MEDIUM` + `CODEX_CLI_PATH` + `CODEX_SANDBOX_MODE` + `CODEX_WORKDIR` + `CODEX_WEB_SEARCH_MODE`) + `app/lifespan.py` `_SUPPORTED_A4_PROVIDERS` 확장 + `_validate_codex_cli()` 신규 (binary 없음 / exit nonzero / timeout 셋 분기). **reasoning_effort fix** (사용자 원래 결정 코드 반영): `openai.py:complete` GPT-5 series 분기에 `payload["reasoning_effort"] = settings.LLM_REASONING_EFFORT_HIGH or _MEDIUM` 추가 (Chat Completions API top-level) + `openai.py:search_with_tools` Responses API nested `payload["reasoning"] = {"effort": ...}` 추가. **infra**: backend/Dockerfile (Node.js 20 + `npm i -g @openai/codex` + `codex --version` sanity) + docker-compose.yml api+worker `${HOME}/.codex:/root/.codex` rw volume mount + Makefile `make codex-login` + `make codex-status` target + `.env.example` x2 LLM_PROVIDER=codex_oauth 권고 + 6 cross-check 모두 통과 (check_env / check_api_docs / check_contracts / check_redis_keys 등). **테스트**: test_codex_oauth.py 14 케이스 (argv 조립 high/medium / response_format=json + `--output-schema` / exit non-zero / no agent_message / binary not found / cached vs live mode / `--search` flag / search response JSON parse error / wrong type / JSONL parser non-JSON skip / multi agent_message 연결 / web_search_call item ignore / usage reasoning_output_tokens 누적) + test_openai_reasoning_effort.py 6 케이스 (gpt-5 high/medium / settings override / 비 gpt-5 omit / responses nested / responses 비 gpt-5 omit) + test_lifespan_provider_guard.py 갱신 (allowed/binary-missing/exit-nonzero 3 케이스). **검증**: ruff All checks passed / mypy --strict 10 files no issues / pytest tests/llm_provider/ 32 passed (Redis 의존 4 error pre-existing) / scripts/check_env exit 0. **docs 갱신**: decisions.md §3 LLM 어댑터 표 정정 + §14 신규 (사용자 결정 6 + 자체 결정 8 + reasoning_effort fix matrix) + AGENTS.md (LLM 어댑터 행 + 작업규칙 §4 §9 정합) + README.md (Why box LLM 라인) + docs/ops/env-vars.md (표 5 행 + example block) + 본 backlog 항목. | `backend/app/llm_provider/{codex_oauth.py, openai.py, __init__.py}`, `backend/app/{config/__init__, lifespan}.py`, `backend/Dockerfile`, `docker-compose.yml`, `Makefile`, `backend/.env.example`, `.env.example`, `backend/tests/llm_provider/{test_codex_oauth.py(신규), test_openai_reasoning_effort.py(신규), test_lifespan_provider_guard.py, test_openai_search.py}`, `docs/decisions.md`, `docs/decision-backlog.md`, `docs/ops/env-vars.md`, `AGENTS.md`, `README.md` |
| C-36. v13 round 3 후속 — integration demo 검증 + 발견 결함 fix (2026-05-16) | round 3 fix 적용 후 실 환경 (docker compose + 실 OpenAI 호출 GPT-5.5) 통합 시연으로 round 1/2/3 fix 26건 검증. **GPT-5.5 + Responses API + web_search**: 12 output items (5 reasoning + 5 web_search_call + 1 message) chain, 26 documents inserted (33 academic_paper / 14 vendor_blog 정상 분류), NFR-25 self-summary 100% 준수, dedup cross-leaf 정상 (PhysForge 등이 2 cluster 매핑). **시연 중 발견 후속 결함 2건 fix**: **R3-C01** `LLM_REQUEST_TIMEOUT_SECONDS=60` 부족 (web_search 호출이 60s 초과 → timeout) → 5곳 모두 180s 로 통일 (config default, .env.example x2, docs/ops/env-vars.md 표+example block). **R3-C02** `documents_service` 의 `SELECT DISTINCT ... ORDER BY coalesce(published_at, created_at)` 가 PostgreSQL "ORDER BY expressions must appear in select list" 위반 → SELECT 에 `sort_ts` label 추가 + row unpacking 4-tuple 로 변경 (_sort_ts 무시). **시연 중 발견 추가 정합 작업**: CSO 3.4.1 다운로드 URL 깨짐 (HTML 반환) → 사용자 직접 다운로드 + `docker cp`. CSO 3.4.1 의 CSV 형식이 N-Triples → csv-quoted (`"<URI>","<P>","label"@en .`) 변경 → parser 를 csv.reader → regex `_TOKEN_RE.findall(line)` 으로 재작성 + `_strip_outer_csv_quote` helper 추가. CSO 3.4.1 에 5 cluster root 라벨 부재 → SEEDS dict + broad_interests.toml 4 entry 교체 (Operating Systems / Automata Theory / Interactive Computer Graphics / Multimedia Systems / Scientific Computing). `_norm_label` 에 underscore→space 정규화 추가. docker-compose.yml postgres 호스트 포트 5432→5433 (native PostgreSQL 충돌 회피). env-vars.md 표 + example block 의 LLM_MODEL_HIGH/MEDIUM gpt-5.5 + CLICKBAIT_ENABLED + LLM_REQUEST_TIMEOUT_SECONDS=180 정합. | `backend/app/{config/__init__, collection/{service, schemas}, llm_provider/openai, topic/{documents_service, cso_importer}, topic/mapping}.py`, `backend/app/config/broad_interests.toml`, `docker-compose.yml`, `.env.example` x2, `docs/ops/env-vars.md` (5곳 통일), `AGENTS.md` (verified), `decision-backlog.md` (본 항목) |
| C-42. A8-v2 UserProfile + Discovery Fusion + Reincarnation Pivot 본문 (2026-05-19) | discovery slot 2 의 본질을 "trust=high trend 정렬" → "Fusion 1 (archive x current cross-product) + Reincarnation 1 (score_tail >= 0.6 archived trace 부활)" 로 pivot. core 5 + adjacent 3 은 안정성 base 그대로 유지. SRS FR-41 "잠재적으로 관심 있을 수 있는" 의 의도 회복. **결정 매트릭스 21건** (decisions.md §15, 사용자 11 + 자체 10): Discovery slot 본질 / core·adjacent 변경 없음 / 5:3:2 유지 / UserProfile ORM·schema 만 (endpoint·UI 없음) / 구조화 6 필드 / score_tail >= 0.6 archive 만 input / daily 19 UTC / LLM_PROVIDER env 재사용 / reasoning high / cold-start = 다중 active trace cross-trace fusion 시도 + 단일 trace fallback / strict output schema / Lua atomic CAS release / per-user try/except + commit / cache-before-commit 회피 / Bridge CSO 매핑 가드 등. **학술 정합**: PersonaX (ACL '25, offline cron + 자유 텍스트 페르소나) + LettinGo (KDD '25, 임베딩보다 자유 텍스트가 transparent) + Guided Profile Generation (한 단락 profile +37% accuracy) + Serendipity 3-dim framework (Fortuitous + Refreshing 의 taste reincarnation + Enriching) + 본 라운드 고유 archive x current cross-product 융합 angle. **PR-stack 7-commit**: PR-1 alembic 0007 + UserProfile ORM + models/__init__ + ck_collection_job_type 7-value (`{daily_collect, leaf_lifecycle, merge_evaluation, summary_generation, interest_decay, trace_merge, daily_user_profile_generation}`) + PR-2 contracts (JobType.DAILY_USER_PROFILE_GENERATION + RedisKey.user_profile_generation_lock + RedisKey.user_profile_cache + ErrorCode.PROFILE_LLM_OUTPUT_INVALID/PROFILE_BRIDGE_CSO_NOT_FOUND) + Settings 7 신규 env (USER_PROFILE_CRON/ARCHIVE_SCORE_TAIL_MIN=0.6/GENERATOR_VERSION="v1"/INPUT_ARCHIVE_MAX=8/REINCARNATION_GAP_DAYS_MIN=7/LOCK_TTL_SECONDS=180/CACHE_TTL_SECONDS=3600) + PR-3 `app/profile/` 5 파일 (`__init__` + `schemas` (Pydantic strict + USER_PROFILE_JSON_SCHEMA) + `config_loader` + `prompt_builder` (한국어 system + user prompt + JSONSchema echo) + `service` (fetch_profile_llm_input + generate_profile_payload + upsert_user_profile + get_user_profile)) + PR-4 worker/jobs/user_profile.py + scheduler.py (7번째 cron 등록 user_profile_generation_job) + PR-5 recommendation/candidates.py (query_discovery_fusion / query_discovery_reincarnation / query_discovery_trend 신규, 기존 query_discovery deprecated alias) + recommendation/engine.py (`_build_discovery_pool_raw` helper + build_dashboard 분기 본문 교체 — fusion → broadening fallback → reincarnation → deepening fallback → trend) + recommendation/reasons.py (NFR-04 거부 키워드 강화 — 버킷/score_tail/신뢰도 추가) + traversal/queries.py (get_archived_traces_with_score / get_top_archived_trace / get_descendant_archived_leaves 3 신규) + PR-6 docs (decisions §15 + 본 backlog C-42 + sdd/contracts JobType/RedisKey/ErrorCode 표 + data/schema UserProfile § + api/recommendation A8-v2 cron 내부 ErrorCode 표 + ops/env-vars A9 § + 7 env 행). **Anti-pattern 회피 9건** (A4 C-02 / A6 C-01·C-02·C-03 / A7 R2-RG-3 / A8 §11.#1·#2·#4 / C-41 / 신규 LLM hallucination CSO 매핑 가드 + 토큰 폭주 cap). **6 cross-check 통과**: check_schema 25 tables / check_contracts CHECK clause / check_env code 131 = .env.example 131 / check_error_codes 45 = 45 / check_redis_keys raw f-string 0 / check_api_docs endpoint 55 = 55. **시연 narrative**: "AI 가 매일 사용자의 archived trace 와 active trace 를 cross-product 해서 두 영역이 만나는 새 학습 path — 예: Graph Algorithms (과거) x Memory Management (현재) = Memory-bounded Algorithms — 를 discovery 카드로 제시. 학문이 가장 크게 도약하는 지점 (ML+Systems=MLSys, HCI+AI=대화형 AI) 의 메커니즘을 개인 추천 차원에서 구현." | `backend/alembic/versions/0007_a9_user_profile.py`, `backend/app/db/models/{user_profile.py, __init__.py}`, `backend/app/{contracts.py, config/__init__.py, scheduler.py}`, `backend/app/profile/{__init__.py, schemas.py, config_loader.py, prompt_builder.py, service.py}`, `backend/app/worker/jobs/user_profile.py`, `backend/app/recommendation/{candidates.py, engine.py, reasons.py}`, `backend/app/traversal/queries.py`, `.env.example`, `backend/.env.example`, `docs/{decisions.md §15, decision-backlog.md C-42 (본 항목), sdd/contracts.md, data/schema.md, api/recommendation.md, ops/env-vars.md}`. **[PR #25](https://github.com/nwejnkasdf/SKKU-insight/pull/25) 머지 commit `63f2cdde` (2026-05-19)**. 단일 commit `71528db` (30+ files). **Codex R1 audit (codex:rescue GPT-5.5)**: Critical 2 + Suggested 7 + Nit 2 = 11 issue (decisions.md §15 "Codex R1 audit + R1 fix" § 참조). **R1 fix 7건 적용**: Critical #1 lock TTL 180→360s / Critical #2 `_build_discovery_pools` tuple 반환 + `_build_fusion_subslot`/`_build_reincarnation_subslot` 분리 + per-source `[:1]` concat (slot 분배 강제) + `_resolve_seed_id` helper / Suggested #1 cache invalidate finally 별도 try/except + committed flag / Suggested #3 trace_path_csos 제외 / Suggested #4 fallback 이 doc rows 기반 (`if rows: return rows`) / Suggested #6 alembic downgrade `NotImplementedError` / Nit #1 prompt 자연어. **P2 backlog 신규 4건**: P2-26 LLMProvider output_schema 인자 / P2-27 archived_at_active_day 별도 컬럼 / P2-28 candidate_pool 매핑 강제 / P2-29 recommendation_cache key version 연결. **테스트 추가**: tests/profile/ 4 파일 (test_schemas / test_prompt_builder / test_service_validation / test_audit_regressions) — **57 passed**. **WSL docker compose 통합 시연**: alembic 0001→0007 통과 (ck_collection_job_type 7-value 정상) + api lifespan 부트 (`cso_graph nodes=14707 edges=44131 clusters=12 / provider=codex_oauth / system_config_loaded=true`) + scheduler `JOB_REGISTRATIONS=7` (user_profile_generation_job 포함) + 모든 신규 모듈 surface import 검증 통과 (`_build_discovery_pools` / `_build_fusion_subslot` / `_build_reincarnation_subslot` / `_resolve_seed_id` / `generate_profile_payload` / `upsert_user_profile` / `get_user_profile` / `fetch_profile_llm_input` / `get_archived_traces_with_score` / `get_top_archived_trace` / `get_descendant_archived_leaves` / `UserProfile` ORM 10 컬럼) + signup/login/consent/clusters HTTP OK. 5 persona × 실 GPT-5.5 fusion 카드 시연은 P2 backlog (broad_interest ID 매핑 + 행동 데이터 SQL seed 필요) |

---

## P1 — Reasonable Default로 진행 (12건 — 활성 8 + 해결 3 + 무효 1)

### P1-1. User 엔티티에 사용자 클래스(학생/연구자/교수) 필드 추가 여부
- **원본**: `algorithms/cold-start.md:15`
- **사용 맥락**: cold-start LLM 입력에 사용자 클래스를 힌트로 넣어 첫 10개 추천을 페르소나에 맞게 생성.
- **default**: **User 테이블에 추가하지 않는다.** 대신 온보딩 화면에서 받은 user_class를 transient input으로 cold-start LLM 호출 1회에만 사용하고 저장하지 않는다. 이후 일반 추천은 행동 로그(베이지안 사후)로 충분히 개인화된다.
- **stub**: `POST /onboarding/interests` ([`api/onboarding.md`](api/onboarding.md)) 요청 바디에 `user_class: "undergraduate" | "researcher" | "professor" | "general"` 필드를 받아 cold-start orchestrator로 transient 전달. User 영구 저장 없음.
- **튜닝 트리거**: 시연 후 사용자 클래스를 영구적으로 보고 싶다는 요구가 생기면 마이그레이션으로 컬럼 추가.

### P1-2. 24시간 cold-start LLM 호출 캡
- **원본**: `algorithms/cold-start.md:118`
- **default**: `recommendation.toml` 의 `cold_start_max_per_day = 100` (전역). 사용자당으로는 사실상 가입 직후 1회만 호출되므로 100/일 전역 캡으로 시연 충분.
- **stub**: 초과 시 fallback 경로 (trust_level=high 트렌드)로 자동 전환. 이미 `runbooks.md` §2 정의됨.

### P1-3. 낚시성 분류기 1회 추론 SLA
- **원본**: `algorithms/clickbait-integration.md:112`
- **default**: **CPU 환경 5초**, 초과 시 비동기 큐 전환. `clickbait-detector` 컨테이너의 `/classify` 엔드포인트는 동기 응답 5초 timeout, 초과 응답은 backend 측에서 RQ 큐로 wrap (`worker`가 후처리, 결과는 `ClickbaitResult.evaluated_at` 사후 채움).
- **stub**: 시연 환경(데이터 5+ 페르소나)에서는 모든 호출이 5초 이내 완료한다고 가정. 비동기 경로는 코드 골격만 두고 활성화는 시연 후.

### P1-4. UserEvent에서 leaf_topic 없는 이벤트의 토픽 분배 정책
- **원본**: `algorithms/interest-bayesian.md:184`
- **default**: **이벤트가 가리키는 Document에 매핑된 모든 (cso_topic, leaf_topic) 쌍의 confidence를 정규화해 분배.** leaf_topic이 없는 매핑은 cso_topic만 분배 대상에 포함. 사용자 명시 이벤트(`not_interested` 등)에서 토픽이 직접 지정된 경우는 100% 단일 분배.
- **stub**: `app/interest/topic_distribution.py` 의 `resolve_topic_distribution(event)` 함수가 위 룰을 구현.

### P1-5. CSO 다운로드 버전
- **원본**: `data/cso-import.md:227`
- **default**: **CSO 3.5** (2026-05-24 C-46 라운드 전환). **git-tracked `data/cso/CSO.3.5.csv`** (25.7 MB, CC BY 4.0 attribution `NOTICE.md`) — clone 만으로 즉시 시연 가능. `make seed-cso-cache` (FILE 생략) 가 본 파일을 컨테이너 `cso_cache` volume 으로 카피. fallback: `CSO_DOWNLOAD_URL=https://cso.kmi.open.ac.uk/downloads/CSO.3.5.csv` env (KMI 서버 다운로드).
- **stub**: `scripts/import_cso.py` 가 다운받은 N3/CSV의 hash를 `cso_metadata` 테이블에 기록 → 향후 버전 갱신 시 변경 감지.
- **튜닝 트리거**: 신버전(3.6+) 출시 시 `data/cso/CSO.X.Y.csv` commit + `Makefile::seed-cso-cache` default FILE 갱신 + `CSO_DOWNLOAD_URL` env 교체 + `make seed-cso-cache && make import-cso ARGS="--reset --refresh"`.
- **본 항목은 closed** (C-46 git-tracked 전환으로 default 안정화). 향후 버전 갱신 시 본 항목 재오픈.

### P1-6. 네이버뉴스 Document 야간 정리 잡 정책 — **무효 + 코드 정리 완료 (v13 라운드 pivot, 2026-05-11 / 2026-05-16)**
- **원본**: `data/schema.md:352`
- **무효 사유**: v13 라운드 A4 pivot 으로 NaverBS4 어댑터 폐기. 네이버뉴스 Document 자체가 시스템에 진입하지 않으므로 정리 잡 불필요.
- **코드 정리 (2026-05-16, A4 본문 PR)**: `backend/app/scheduler.py` 의 `JOB_REGISTRATIONS` 에서 `naver_cleanup_job` dict 제거. `backend/app/worker/jobs/naver_cleanup.py` docstring 에 "v13 폐기" 마킹 (파일 자체와 `NAVER_CLEANUP_CRON` env 는 향후 News 재활성화 위해 보존). `tests/collection/test_audit_regressions.py::test_naver_cleanup_not_in_scheduler` 회귀 가드.
- **본 항목은 closed.**

### P1-7. SRS docx 와이어프레임 PNG 추출 경로
- **원본**: 본 정리 작업에서 신규 식별. SRS 분할 파일과 `ux/wireframes.md` 의 PNG 링크 부재 안내(이미 본 정리 작업으로 박스 추가됨).
- **default**: **PNG 추출 안 함**. `ux/wireframes.md` 의 화면별 상태 머신 Mermaid + 정상/빈/오류 매트릭스를 단일 권위 소스로 사용. A9는 본 Mermaid·매트릭스만 보고 6개 화면을 구현한다.
- **stub**: 추후 발표용 슬라이드에 와이어프레임 이미지가 필요하면 `unzip SKKU_InSight_SRS.docx -d /tmp/srs-docx && cp /tmp/srs-docx/word/media/* assets/` 로 추출 (본 저장소 외부 한정).

### P1-9. NetworkX `find_adjacent` 그래프 단계 cap 부재 (자체감사 B-2)
- **원본**: A3 자체감사 (opus 4.7) — `backend/app/topic/graph.py:103-126`
- **현황**: `cso_service` 단계에서 `[:MAX_TOPICS_IN_RESPONSE=500]` 슬라이스 — 응답 cap 적용. 그러나 `find_adjacent` 그래프 BFS 자체는 cap 없음. hops=3 + AI cluster 루트 같은 densely-connected seed 시 visited set 수천 노드 팽창 가능. Latency · 메모리 사용 예측 불가.
- **default action**: 1차 시연 (10-20 사용자) 영향 작음. 운영 단계에서 `find_adjacent(g, seed, hops, max_visited=2*MAX_TOPICS_IN_RESPONSE)` 도입 후 early break.
- **시점**: 운영 진입 직전. backlog.

### P1-11. `/topics/cso/clusters` cache miss single-flight (Codex 2nd 감사 B-2)
- **원본**: `backend/app/topic/cso_service.py:get_clusters`
- **현황**: Redis cache invalidate 직후 동시 호출 시 모두 cache miss → 동시 DB JOIN + SETEX → thundering herd. 12 행 JOIN 이라 시연 Critical 아님.
- **default action**: `RedisKey.cluster_cache_lock()` `SET lock NX EX 5` 도입. lock 보유자만 DB → SETEX. 미보유자는 await cache + 재확인 후 응답. 운영 환경 진입 시 도입.
- **시점**: 운영 단계 (10-20 명 시연 영향 없음).

### P1-10. CSO 재임포트 후 Redis cache invalidate 실패 정책 (자체감사 C-1)
- **원본**: A3 자체감사 — `backend/scripts/import_cso.py:144-145`
- **현황**: `import_cso.py` finally 에서 `RedisKey.cso_clusters_cache()` DEL. 실패 시 warn + 무시 — 24h TTL 후 자연 만료. `cache.py:33` prefix `v1` versioning 으로 부분 완화.
- **default action**: invalidate 실패 시 3회 backoff retry. 모두 실패 시 RuntimeError (운영자가 인지하고 수동 invalidate). 또는 `RedisKey.cso_clusters_cache()` versioning 을 `v2` 로 bump.
- **시점**: 운영 단계 결정.

### P1-8. vLLM의 DoRA 어댑터 호환성 + A.X-4.0-Light 로드 검증 — **해결됨 (2026-05-11)**
- **원본**: `algorithms/clickbait-integration.md` §서빙 엔진(vLLM)
- **결정**: stub 옵션 (a) 채택. **DoRA scaling을 사전에 base에 merge → vLLM이 일반 base로 로드** (multi-LoRA serving 미사용). vLLM의 DoRA 직접 지원 여부와 무관하게 동작. base 모델은 [skt/A.X-4.0-Light](https://huggingface.co/skt/A.X-4.0-Light) 공식 모델 카드의 vLLM 서빙 예시로 호환성 가정.
- **구현**: `clickbait_module/scripts/merge_adapter.py` (peft `merge_and_unload()` + `chat_template.jinja` + `run_meta.json` 복사), `clickbait_module/app/inference.py` (vLLM `AsyncLLMEngine` 부트).
- **잔여 작업**: GPU 환경에서 1회 머지 + 부트 + 학습 평가 1건 sanity check (코드 외부 운영 작업).

### P1-12. Trace operation extend/split/archive caller 누락 — A7 미완성 발견 → **해소 (2026-05-20, C-45 라운드, PR #30 머지)**
- **발견 경위**: 30일 day-by-day 시뮬레이션 (frontend 브랜치 worktree, 사용자 fd81e703 + AI/security trace 6개) 도중 `user_cso_traversal.path` 가 30일 내내 길이 1 유지. grep 으로 `evaluate_extend` / `execute_extend` (그리고 split) 가 `app/traversal/` 안 정의만 있고 production code 어디서도 호출 안 됨을 확인.
- **원래 상태표 (fix 전)**:
  - ✅ `evaluate_retract` — `worker/jobs/daily_lifecycle_evaluation.py:_evaluate_trace_demotion_for_user` 정상 호출
  - ✅ leaf-level `execute_merges` — `leaf_lifecycle.hybrid_d.evaluate_merges` 정상 호출
  - ❌ `execute_extend` / `evaluate_extend` — 0 production caller (Settings `TRACE_EXTEND_MIN_INTERACTIONS=5` 도 미사용)
  - ❌ `execute_split` / `evaluate_split` — 0 production caller
  - ⚠️ `execute_archive` — grep false negative. 실제로는 protocol 메서드명이 `archive_if_eligible` (caller 가 `_evaluate_trace_demotion_for_user`) 였음. 즉 archive 는 caller 존재 + grep 키워드 차이로 인한 오탐.
  - 단위 테스트 `tests/traversal/test_audit_regressions.py::test_execute_extend_uses_array_append` 는 `inspect.getsource` 정적 검증만이라 통합 caller 부재 detect 안 됨
- **영향 (fix 전)**: 사용자 `trace.path` 가 자연 확장 불가능 → UI 시각화 (관심 경로, docTraceMini) 가 root 노드 1개 고정. A8-v2 user_profile_generation 의 archive trace fusion 도 archive 진입 자체가 안 일어나서 무력화. 시연/평가에서 traversal 핵심 narrative 결손.
- **Why**: A7 PR-stack 머지 시 Codex 3 라운드 audit 가 extend/split caller 누락을 놓침 (test 가 정적 패턴 검증만이라서). AGENTS.md / decisions.md §12 의 "A7 ✅ 완료" 표기는 함수 정의 + 단위 테스트 기준이지 통합 동작 기준이 아니었음 — 사실상 **docs drift**.
- **Resolution (2026-05-20, [PR #30](https://github.com/nwejnkasdf/SKKU-insight/pull/30) 머지, commit `19d780c` + `2b85714`)**:
  - **caller 신규** — `daily_lifecycle_evaluation_job._evaluate_trace_expansion_for_user` (`backend/app/worker/jobs/daily_lifecycle_evaluation.py:50-165`) 가 demotion 평가 직전 호출. 각 active trace path 끝 cso 의 graph 자식 cso 중 임계 통과 자식 조회 → 1개 통과 시 `engine.evaluate_extend(trace_id, child_cso)`, ≥2개 통과 시 `engine.evaluate_split(trace_id, [top1, top2])` (T 가 child_A 로 extend, T' 가 child_B 로 fork — A7 결정 #20 정합). 임계: `Settings.TRACE_EXTEND_MIN_INTERACTIONS` (default 5, 본 fix 가 첫 실사용). Loop guard: 이미 path 안에 있는 child 는 skip. trace 당 day max 2 자식 처리 (1 extend or 1 split).
  - **active_day window** — alembic 0009 + `UserEvent.active_day_at_event INTEGER NULL` 컬럼 신규. extend window 계산을 벽시계 7d → SRS 시간모델 SOR (active day delta `(user.active_day_counter - active_day_at_event) <= 7`) 정합. `interest.service._record_user_event` 시그니처에 `active_day_at_event` 추가, `ingest_event_atomic` 가 `active_day` 인자 전달 → 모든 신규 user_event INSERT 가 자동으로 컬럼 채움. 기존 row 는 NULL (caller 가 window 밖 취급).
  - **event_type filter 제거** — 모든 type 카운트 (view/click/save/hide/not_interested/dwell_tick).
  - **운영 도구 신규** — `backend/scripts/simulate_user_day.py` (argparse + asyncio + fresh subprocess per job, 3 명령: next-day / collection / weekly, before/after 스냅샷 출력) + Makefile target 3종 (next-day / collection-user / weekly-user, EMAIL=... 또는 USER_ID=... 인자) + `docs/ops/manual-day-control.md` (의도/명령/사용/출력예/흐름권장/안전장치/한계/디버그 8 section). P1-12 fix 검증 + 시연 narrative 보조용 수동 day 진전.
  - **live 검증** (gywndrnjs123@naver.com, ad=93→94): traces 4→5, leaves 6→0, max_path_len 1→2, daily_lifecycle_evaluation_job 결과 `extended=2 split=0 retracted=1 archived=4 leaf_demoted=0` — extend/split/archive 모두 production 동작 확인.
- **Before-fix snapshot**: `A9-before-fix-extend` 브랜치 — 30일 시뮬레이션 직후 + 사전 fix 4건 (codex_oauth `_SEARCH_OUTPUT_SCHEMA` strict 통과 + `_try_parse_json_relaxed`, leaf_lifecycle prompt 에 doc title/summary enrich, interest_decay `COALESCE` NULL-safe, client `App.tsx` docTraceMini 가 `path_labels` 받게).
- **본 항목은 closed.**

---

## P2 — 폴리시·검증 단계 (7건, 활성 5 + 해결 2)

### P2-1. OpenAPI 자동 생성 결과 cross-check
- **원본**: `api/auth.md:82`
- **default action**: A2가 FastAPI 부트스트랩 후 `python -m app.openapi_dump` 로 OpenAPI YAML을 출력하고, `docs/api/*.md` 의 시그니처와 자동 비교하는 단순 diff 스크립트를 `scripts/check_api_docs.py` 에 둔다. CI에서 비교.
- **시점**: A2 작업 마지막 단계.

### P2-2. 시드 페르소나 문서 ID 캡처
- **원본**: `data/seed-personas.md:162`
- **default action**: A12가 시드 스크립트 1회 실행 후 생성된 Document ID를 `scripts/fixtures/seed_documents.json` 에 캡처해 시연 재현성 확보.
- **시점**: A12 작업 + A11 (test-ci) 가 동일 fixture 사용.

### P2-3. 빅테크 RSS URL 검증 — **무효 (v13 라운드 pivot, 2026-05-11)**
- **원본**: `data/sources-registry.md` 의 `# TODO: verify URL` 약 22개
- **무효 사유**: v13 라운드 A4 pivot 으로 RSS 어댑터 폐기. sources-registry 의 빅테크 RSS 목록 자체가 의미를 잃음 (sentinel 1행 `llm_search` 통일). **본 항목은 closed.**

### P2-4. 빅테크 소스 50–80개로 확장 — **무효 (v13 라운드 pivot, 2026-05-11)**
- **원본**: `data/sources-registry.md:392`, `:482`
- **무효 사유**: v13 라운드 A4 pivot 으로 source registry 의 빅테크 목록 폐기. publisher 정보는 `Document.raw` JSONB (schema.md ORM 컬럼명) 로 동적 보관. 확장 개념 자체가 LLM 검색 모델에서 의미 없음. **본 항목은 closed.**

### P2-5. 관리자가 사용자 비밀번호를 강제 reset 하는 임시 endpoint
- **원본**: `security/password-policy.md:111`
- **default action**: **시연 동안은 미구현.** 사용자가 잊으면 `docker compose exec api python -m app.scripts.reset_password user@example.com` CLI로 직접 임시 비번 발급. 운영 단계에서 endpoint 검토.
- **시점**: 본 1차 구현 범위 외.

### P2-6. clickbait-detector 외부 인터페이스 가정 — **해결됨 (2026-05-11)**
- **원본**: `algorithms/clickbait-integration.md:107` 영역 (해소되어 마커 정리됨)
- **결정**: P0-1 해결과 함께 자연 해소. 모듈 위치 = `clickbait_module/`. 서빙 엔진 = vLLM. 호스팅·transport는 운영 결정. 자세히는 P0-1 + P1-8 + P2-7.

### P2-8. `list_traces.leaf_count` N+1 쿼리 (자체감사 B-1)
- **원본**: `backend/app/topic/trace_service.py:124-144`
- **현황**: `for r in page:` 루프 안 trace 마다 count_stmt 별도 실행. limit=20 시 20 round-trip.
- **default action**: `select(UserCSOTraversal.trace_id, func.count(...)).group_by(trace_id)` 한 번 batch. 또는 `array_overlap` GIN 인덱스 활용.
- **시점**: A7 데이터 채워진 후 응답 latency 측정 결과 따라 결정.

### P2-9. `_summarize_topics` 정렬 비결정성 (자체감사 B-3)
- **원본**: `backend/app/topic/cso_service.py:181-191`
- **현황**: `select(...).where(in_(topic_ids))` DB 반환 순서 미지정. adjacent / descendants 응답 topics list 호출마다 다른 순서 가능 — UI 깜빡임.
- **default action**: `order_by(CSOTopic.label)` 또는 입력 순서 유지 `case(...)` 추가.
- **시점**: A8-v2 (electron-client) UI 정렬 룰 확정 후.

### P2-10. `--reset` 후 stale `user_cso_traversal.path` UUID — **해소 (2026-05-19)**
- **원본**: `backend/app/topic/cso_importer.py::reset_cso_tables`
- **결과**: P2-16 과 함께 `reset_cso_tables(session, force_orphan=False)` 가드 추가. `dynamic_leaf_topic` 또는 `user_cso_traversal` 행이 존재하면 default 거부 (RuntimeError + 카운트 메시지). 운영자가 의도 명시 시 `scripts/import_cso.py --force-orphan-cso-refs` 플래그로 우회. 시연 환경 (빈 DB) 은 영향 없음.
- **회귀 가드**: `backend/tests/topic/test_audit_regressions.py` 의 5 신규 테스트 — 정적 시그니처 검증 1 + 동적 mock 분기 3 + CLI 플래그 검증 1.
- **본 항목은 closed.**

### P2-11. `find_equivalents` 단방향 매칭 (자체감사 B-7)
- **원본**: `backend/app/topic/graph.py:143-147`
- **현황**: `g[seed_id].items()` 가 outgoing edge 만. equiv 가 `a→b` 단방향 저장 시 `find_equivalents(b)` 빈 응답. 1차 PRED_REL 미사용 (cso_importer.py:108 무시) — latent bug.
- **default action**: `out_edges` + `in_edges` 양쪽 검사 (또는 import 시 equiv 양방향 엣지 추가).
- **시점**: relatedEquivalent 활성화 시 (P3).

### P2-12. CSO importer 단일 transaction WAL 부담 (자체감사 B-9)
- **원본**: `backend/app/topic/cso_importer.py:113-152`
- **현황**: Pass 1 (14k INSERT) + Pass 2 (14k UPDATE + 14k+ M:N INSERT) 모두 같은 transaction. `await session.commit()` 마지막 한 번. WAL · undo log 압박.
- **default action**: Pass 1 commit → Pass 2 별도 transaction. 또는 chunk 단위 (1000 row) commit.
- **시점**: 임포트 시간 측정 후 5분 SLA 위배 시 또는 운영 환경.

### P2-13. CSO graph cycle 영향 노드 관찰성 (자체감사 B-10)
- **원본**: `backend/app/topic/graph.py:80-94 verify_cso_import`
- **현황**: cycle 발견 시 첫 cycle 의 5 노드 미리보기만 WARN log. cycle 멤버 전체 list / prometheus metric 미노출 → 운영자가 영향 노드 파악 어려움.
- **default action**: cycle 발견 시 영향받는 노드 list 를 structlog binding + cycle metric 노출. 또는 cycle 멤버 cluster_labels 일시 제외.
- **시점**: 운영 단계 monitoring 도입과 함께.

### P2-14. `CSO_DOWNLOAD_TIMEOUT_SECONDS` env 노출 (자체감사 C-2)
- **원본**: `backend/app/topic/cso_importer.py:72`
- **현황**: httpx.stream timeout=60.0 hardcoded. 캠퍼스 회선 5s 이내지만 슬로우 회선 + ssl handshake 시 부족 가능.
- **default action**: BaseSettings 필드 `CSO_DOWNLOAD_TIMEOUT_SECONDS=60` 노출 (`.env.example` + `env-vars.md` 동시). 또는 hardcoded 유지.
- **시점**: 운영 회선 측정 후.

### P2-16. `--reset` 후 DynamicLeafTopic.cso_topic_ids 빈 배열 — **해소 (2026-05-19)**
- **원본**: `backend/app/topic/cso_importer.py::reset_cso_tables` 부작용 (A-2 fix 의 trade-off)
- **결과**: `reset_cso_tables(session, force_orphan=False)` 가드 추가. 본 함수 진입 시 `SELECT count(*) FROM dynamic_leaf_topic` + `... user_cso_traversal` 두 카운트 — 둘 중 하나라도 > 0 + `force_orphan=False` 면 RuntimeError (P2-16 + P2-10 메시지 동시). `scripts/import_cso.py` 가 `--force-orphan-cso-refs` CLI 플래그로 우회 노출. 시연 환경 (빈 DB) 은 영향 없음 — 카운트 0 이면 즉시 정상 진행.
- **회귀 가드**: `backend/tests/topic/test_audit_regressions.py` 의 5 신규 테스트 (P2-10 항목 참고).
- **본 항목은 closed.**

### P2-17. inspect.getsource 정적 검증 → DB fixture 통합 테스트 (Codex 감사 E)
- **원본**: `backend/tests/topic/test_audit_regressions.py` A-1·A-2·B-6·Codex Critical 회귀 테스트
- **현황**: 정적 source inspection 패턴 (`inspect.getsource + 키워드 검색`) 이 변수명 변경·주석 삽입·문자열 보간 등으로 우회 가능. 동적 mock 기반 (A-3·A-4·B-2·B-3·B-4) 보다 회귀 신뢰성 낮음.
- **default action**: A7 (leaf 데이터) 머지 후 실제 DB fixture 로 active/stale/merged leaf 4개 삽입 → list_traces.leaf_count == len(get_trace_detail.leaves) 통합 검증. cso_topic_parent 다중 부모 시드 → CSOTopicDetail.parents 다중 검증. docker compose 환경 (A11).
- **시점**: A11 (test-ci) 또는 A7 머지 직후.

### P2-18. CSO importer `parent_topic_id` row-by-row UPDATE → bulk (Codex 2nd 감사 B-4)
- **원본**: `backend/app/topic/cso_importer.py:182-205`
- **현황**: 14k row 의 `parent_topic_id` 가 개별 `await session.execute(update(...))` 로 처리 — 단일 transaction 내에서 14k round-trip.
- **default action**: `UPDATE cso_topic AS c SET parent_topic_id = v.parent FROM (VALUES (...)) AS v(id, parent) WHERE c.cso_topic_id = v.id` bulk UPDATE. 또는 CASE 표현식 1회 호출.
- **시점**: import 시간 측정 후 5분 SLA 위배 시 (P2-12 단일 transaction WAL 부담 과 함께 운영 진입 전).

### P2-19. docker-compose `cso_cache` volume 미선언 — **해소 (2026-05-19)**
- **원본**: `docker-compose.yml` volumes 섹션 + api service volumes
- **결과**: docker-compose.yml `volumes:` 섹션에 `cso_cache: {}` 추가 + api service `volumes:` 에 `cso_cache:/app/.cache/cso` 마운트. 컨테이너 재시작 후에도 CSV 영속화. 동시에 CSO 3.4 → **3.4.1 전환** (URL/캐시 파일명/docs/config 모두 동기). 호스트 사용자 파일을 컨테이너 캐시에 카피하는 `make seed-cso-cache FILE=...` Makefile 타깃 신규 — 오프라인 시연 + KMI 서버 트래픽 절감.
- **본 항목은 closed.**

### P2-20. inspect.getsource 정적 audit test → DYNAMIC 전환 (Codex 2nd 감사 E)
- **원본**: `backend/tests/topic/test_audit_regressions.py` 의 정적 검증 6 테스트
- **현황**: file read · inspect.getsource 패턴 (Codex Critical, B-1, B-3, B-4, A-1, A-2) 가 코드 refactor 시 false positive/negative 위험. 본 라운드에 cache hit 11 cluster bypass (Codex 2nd Critical) 는 정적 테스트가 통과시켰음 (실제 새 dynamic 테스트가 발견). 동적 (HTTPException 검증, Pydantic introspection, AsyncMock) 보다 신뢰성 낮음.
- **default action**: A11 (test-ci) 단계에서 DB fixture 통합 테스트로 모두 교체. e.g. active/stale leaf 4개 삽입 → `len(get_trace_detail.leaves) == list_traces.leaf_count` 통합 검증. 본 라운드는 cache hit 부분만 DYNAMIC 으로 전환 완료.
- **시점**: A11 머지.

### P2-21. alembic 0004 의 `ck_collection_job_type` CHECK 갱신 누락 — **해소 (2026-05-19 검증)**
- **원본**: `backend/alembic/versions/0003_a4_collection_tables.py:180-183` + `0004_a6_interest_tables.py` (CHECK 갱신 부재) vs `backend/app/contracts.py::JobType` (interest_decay 포함 5종)
- **결과**: alembic 0005 (A7, `0005_a7_traversal_merge.py:65-74`) 가 `ck_collection_job_type` DROP + 6-value 재생성 (`daily_collect, leaf_lifecycle, merge_evaluation, summary_generation, interest_decay, trace_merge`). 추가로 0007 (A8-v2, `0007_a9_user_profile.py:92-103`) 가 `daily_user_profile_generation` 추가 → 현재 7-value. 본 항목 close.
- **본 항목은 closed.**

### P2-25. tests/recommendation/ pytest fixture nested connection 충돌 (A8 R3 시연 발견, 2026-05-17)
- **원본**: `backend/tests/conftest.py:db_session` — function 단위 트랜잭션 (`begin()` + `rollback()`) 안에서 service 함수가 별도 await 시도 시 asyncpg `cannot perform operation: another operation is in progress`.
- **현황**: pytest tests/recommendation 실행 — 28 passed / 3 failed / 19 errors. errors 모두 fixture race (single connection 안 동시 await). 본 code 자체 결함 X — fixture 결함.
- **default action**: conftest 의 `db_session` fixture 를 SQLAlchemy 공식 nested transaction (SAVEPOINT) 패턴으로 교체 — outer `connection.begin()` + inner `connection.begin_nested()` + `after_transaction_end` 리스너로 SAVEPOINT 재생성. service 의 `session.commit()` 이 SAVEPOINT release 만 → fixture 종료 시 outer rollback 으로 격리 보장. A11 (test-ci) 단계에서 conftest 전반 정비와 함께.
- **추가 3 failed test 결함**: test_fr42_slot_short_borrows_from_donor (assertion mismatch) / test_summary_404_when_document_not_exist (fixture 결함 동반) / test_document_topic_cso_ids_empty_for_unknown_doc (assertion mismatch) — A11 에서 fixture fix 후 재검증.
- **영향**: A8 코드 자체 동작 정합 — R3 시연 검증 통과. tests 통과율 28/31 (fixture errors 제외) = 90%.
- **본 라운드 (2026-05-19, P2 백로그 검토) 결정**: A11 머지 시점 유지. **부분 fix 회피** — conftest 가 모든 테스트 모듈의 공유 의존성이라 부분 변경이 기존 90% green 테스트를 깨뜨릴 위험. A11 단계에서 conftest 전반 (db_session + redis_client + client + savepoint 재시작 리스너) 일관 정비.
- **시점**: A11 (test-ci) 머지 직전.

### P2-22. cold_start_orchestrator concurrent race (R2 Critical #1, 2026-05-17)
- **원본**: `backend/app/recommendation/cold_start.py:run_cold_start`
- **현황**: `onboarding_lock` TTL=30s 만료 후 동일 user 두 cold-start POST → 2 worker enqueue → 동시 실행 가능. `_count_cold_start_attempts` 가 atomic 아니라 cap=3 우회 가능 (4 row INSERT 됨).
- **영향**: 1차 시연 (UVICORN_WORKERS=1 + RQ single worker) 영향 작음 — sequential 처리. multi-worker 환경에서 실 race 발생.
- **default action**: contracts.py `RedisKey.cold_start_worker_lock(user_id)` 신규 + worker 진입 시 Lua atomic SET NX EX=180s acquire. 또는 PostgreSQL `pg_advisory_xact_lock(hashtext('cold_start:'||user_id))` 적용.
- **시점**: 운영 단계 (multi-worker 도입 직전).

### P2-23. daily UNIQUE functional index volatility — **해소 (R3 시연 검증, 2026-05-17)**
- **원본**: `backend/alembic/versions/0006_a8_recommendation_tables.py:84` — `CREATE UNIQUE INDEX ux_recommendation_user_doc_slot_day ON recommendation (user_id, document_id, slot_type, ((created_at AT TIME ZONE 'UTC')::date))`
- **결과**: R3 통합 시연 (docker compose + PostgreSQL 16-alpine) 에서 `alembic upgrade head` 0001→0006 정상 통과. functional expression index 가 받아들여짐. fallback patch (`created_day_utc DATE GENERATED ALWAYS AS ... STORED` 컬럼) 불필요.
- **본 항목은 closed.**

### P2-24. `_is_cold_start` 재진입 정책 (R2 Discussion #2, 2026-05-17)
- **원본**: `backend/app/recommendation/engine.py:_is_cold_start` — `active_traces == 0` AND `boost_applied_at_active_day IS NULL` AND `long_alpha <= 2.0` 모두 충족 시 cold-start
- **현황**: 14-day boost 만료 (boost_applied_at_active_day → NULL 으로 차감) 후 사용자가 행동 신호 미발생 시 cold-start 재진입 가능. 정책 의도 (재활성화 cold-start = cold-start.md §적용 조건 "마지막 활동 60 active days 이상") 와 정합? 또는 명시 "cold_start_completed_at" marker 필요?
- **default action**: cold-start.md §적용 조건 "재활성화" 패턴이 본 동작과 일치 — 의도된 정책으로 결정. 단 60 active days 임계 적용 시 명시 marker 필요. 1차 시연은 본 묵시 정책 유지.
- **시점**: M4 발표 후 운영 단계.

### P2-15. UUID v7 (time-ordered) cursor pagination (자체감사 C-3)
- **원본**: `backend/app/topic/leaf_service.py / trace_service.py` cursor
- **현황**: leaf_id / trace_id 가 UUID v4 — cursor tie-break 시 lexicographic 순서 (안전하지만 의미 없음). v7 (time-ordered) 사용 시 더 자연 정렬.
- **default action**: 1차 시연 UUID v4 유지. M4 발표 후 v7 도입 검토 — 모든 모델 PK default 일괄 변경.
- **시점**: M4 후속.

### P2-26. A8-v2 LLMProvider.complete 시그니처 확장 — output_schema 인자 (Codex R1 Suggested 2026-05-19)
- **원본**: `backend/app/llm_provider/protocol.py:78`, `backend/app/profile/service.py:373`
- **현황**: A8-v2 의 USER_PROFILE_JSON_SCHEMA 가 prompt echo + Pydantic 후처리 검증만 — provider API 레벨 strict mode 미연결. 즉 LLM 이 invalid JSON 반환 시 전체 호출 후 fail.
- **default action**: 1차 시연 본 안전 — Pydantic ValidationError 가 graceful fallback. 운영 단계 LLMProvider.complete 시그니처에 `output_schema: dict[str, Any] | None` 인자 추가 + codex_oauth `--output-schema` / openai `response_format={"type":"json_schema","strict":true}` 매핑.
- **시점**: M4 후속 + 모든 provider 변경 영향.

### P2-27. A8-v2 archived trace 의 archived_at_active_day 별도 컬럼 — **해소 (2026-05-19, C-44)**
- **원본**: `backend/app/db/models/user_cso_traversal.py`, `backend/app/traversal/queries.py:get_top_archived_trace`
- **결과**: alembic 0008 으로 `user_cso_traversal.archived_at_active_day INTEGER NULL` + 인덱스 추가. `operations.execute_archive(active_day_counter)` 인자 추가 + `execute_merge` 의 loser archive 시 동일 컬럼 저장. `queries.get_top_archived_trace` + `get_archived_traces_with_score` 가 `COALESCE(archived_at_active_day, last_activity_active_day)` fallback chain 으로 신규 archive 는 정확 비교, 0008 이전 row 는 fallback (backward-compat). default.py 의 `_resolve_active_day_counter` helper 가 User SELECT 로 active_day_counter 조회 — protocol.py 시그니처 변경 회피.
- **회귀 가드**: `backend/tests/profile/test_audit_regressions.py::TestP2_27_ArchivedAtActiveDay` 의 7 신규 테스트 (컬럼 / 인덱스 / 시그니처 / 저장 로직 / queries COALESCE / ArchivedTraceSummary fix).
- **본 항목은 closed.**

### P2-28. A8-v2 fusion bridge_cso 가 candidate_pool 매핑 강제 — **해소 (2026-05-19, C-44, 옵션 A2)**
- **원본**: `backend/app/profile/prompt_builder.py`, `backend/app/profile/service.py`
- **결과**: 옵션 A2 (카테고리별 분리) 채택. `profile/schemas.py::CSOTopicCandidatePool` 신규 — fusion / deepening / broadening 3 카테고리 list 분리. `_build_cso_candidate_pool` 반환 타입 변경 + 카테고리별 다른 풀 (fusion=union, deepening=active path + tail successors, broadening=archive path + tail predecessors). `generate_profile_payload` validation 강화 — 응답 각 카테고리 ID 가 자기 카테고리 풀 안에 있는지 확인 (이전: cso_graph 전체 멤버십만). alembic 0008 으로 `user_profile.candidate_pool_ids JSONB NOT NULL DEFAULT '{}'` 추가. `upsert_user_profile(candidate_pool=...)` 인자로 LLM 이 사용한 카테고리별 풀 영속화. prompt 도 `cso_candidate_pool.fusion` / `.deepening` / `.broadening` 명시.
- **회귀 가드**: `backend/tests/profile/test_audit_regressions.py::TestP2_28_CandidatePoolCategorized` 의 7 신규 테스트 (schema 카테고리 / 반환 타입 / validation / upsert / worker pass-through / prompt 카테고리 명시).
- **본 항목은 closed.**

### P2-29. A8-v2 recommendation_cache key 에 UserProfile.generated_at 버전 연결 (Codex R1 Nit 2026-05-19)
- **원본**: `backend/app/recommendation/engine.py:596`, `backend/app/contracts.py:RedisKey.recommendation_cache`
- **현황**: profile cron commit + cache DEL 이 dashboard build (old profile 읽음) → cache SETEX 와 race window 존재 (multi-worker). 본 race 는 short window (cron 19 UTC + dashboard 호출 동시) — single-worker 시연 환경에선 실효 미발생.
- **default action**: 1차 시연 무시. 운영 단계 multi-worker 도입 시 cache key 또는 value 에 `UserProfile.generated_at` ISO 토큰 포함 + read 시점 stale 거부.
- **시점**: M4 후속.

### P2-7. vLLM 기반 추론에서 next-token "0"/"1" logprob 추출 방식 — **해결됨 (2026-05-11)**
- **원본**: `algorithms/clickbait-integration.md` §서빙 엔진(vLLM)
- **결정**: `SamplingParams(max_tokens=1, logprobs=K, temperature=0.0)` greedy 추론 후 첫 번째 생성 토큰의 logprob 분포에서 id0=56, id1=57 추출 → 2-class softmax. K=20 default(env `LOGPROBS_TOPK`).
- **구현**: `clickbait_module/app/inference.py` (`ClickbaitEngine.classify`). id0/id1은 `adapter/run_meta.json` 권위 사용 (없으면 토크나이저 폴백 + warning).
- **잔여 작업**: 학습 시점 transformers 기반 산식(p0/p1/score_logit_diff)과 vLLM 추론 결과 sanity check 1건 (GPU 환경에서, NFR-09 검증과 함께).

---

## 요약 표

| 우선순위 | 건수 | 차단 에이전트 | 처리 방식 |
|---|---|---|---|
| P0 | 0 (해소됨) | (없음) | 모두 해결 — 모든 에이전트 진행 가능 |
| P1 | 12 (해결 3 — P1-5 + P1-8 + P1-12 2026-05-20/24, 무효 1 — P1-6 v13 pivot, 활성 8 — P1-1·P1-2·P1-3·P1-4·P1-7·P1-9·P1-10·P1-11) | (없음) | reasonable default + stub |
| P2 | 28 (해결 9 — P2-23 A8 R3 시연 + P2-21 alembic CHECK + P2-19 cso_cache + P2-16/10 reset 가드 2026-05-19 + **P2-27 archived_at_active_day + P2-28 candidate_pool_ids 카테고리별 2026-05-19 (C-44)**, 무효 2 — P2-3·P2-4 v13 pivot, 활성 17 — P2-22/24/25 A8 운영·A11 단계 + P2-26/29 A8-v2 R1 audit 잔여 + 기타) | (없음) | 후속 폴리시 단계 |
| C-급 (인터뷰 식별 + codex v1·v2·v3 + multi-worker + 옵션 B + v13 pivot + A4 코드 + 3-라운드 audit + A6 본문 + Codex 2-라운드 audit + A7 본문 + Codex 3-라운드 audit + A8 본문 + R1 self-review + A8-v2 본문 + Codex R1 audit + P2 백로그 그룹 A+B + 그룹 C + P1-12 fix + CSO 3.5 git-tracked) | 46 (해결 46 — C-2 부분 해소, C-6~32 신규 해결 A2, C-33 v13 pivot 2026-05-11, C-34/C-35/C-36 A4 코드 + Codex round 2/3 + 통합 시연 fix 2026-05-16~17, C-37/C-38 A6 본문 + Codex round 1/2 fix 2026-05-17, C-39 A7 본문 + Codex round 1/2/3 fix 2026-05-17, C-40 A8 본문 + R1 self-review 2026-05-17, C-41 CodexOAuth 본문 + reasoning_effort fix 2026-05-18, C-42 A8-v2 UserProfile + Discovery Fusion + Reincarnation Pivot + Codex R1 audit + R1 fix 2026-05-19, C-43 P2 백로그 그룹 A+B 검토 — P2-10/16/19/21 close + CSO 3.4 → 3.4.1 전환 + --reset 가드 2026-05-19, C-44 P2 백로그 그룹 C 일부 — P2-27 archived_at_active_day + P2-28 candidate_pool_ids 카테고리별 분리 close 2026-05-19, C-45 A7 P1-12 fix — extend/split caller 보강 + alembic 0009 active_day_at_event + simulate_user_day.py + manual-day-control.md + AGENTS.md A7 ✅ 전환 + A9/A10 진행상황 반영 2026-05-20 [PR #30](https://github.com/nwejnkasdf/SKKU-insight/pull/30), C-46 CSO 3.4.1 → 3.5 + git-tracked `data/cso/CSO.3.5.csv` (25.7 MB, CC BY 4.0) + NOTICE.md attribution + `make seed-cso-cache` default FILE + Makefile/config/.env.example/docker-compose/13 docs 일괄 정합 + P1-5 close 2026-05-24, C-47 Docker = WSL only 환경 규칙 2026-05-24, C-48~C-52 사용자 시연 대응 fix (cold_start polling + RQ timeout + CORS + sentinel source 제거 + published_at ISO8601 + dev mount/reload + recency slot 별 freshness + discovery decay 제거) 2026-05-24, **C-53 discovery 알고리즘 대규모 공사 — Fusion bridge meet-in-the-middle BFS + Reincarnation softmax sampling + weekly_promotion_job + Recommendation origin metadata + alembic 0010 + 8 cron 등록 + 5 commit-stack 2026-05-24 [PR #39](https://github.com/nwejnkasdf/SKKU-insight/pull/39) + [PR #40](https://github.com/nwejnkasdf/SKKU-insight/pull/40)**, **C-53 followup 평가 fix 4건 — BFS 표준 bidirectional 패턴 + active_trace 도 softmax + weekly_promotion traversal_lock + `[freshness.discovery]` sub-table 폐기 + `_UNITY_FRESHNESS` 코드 상수 2026-05-24 [PR #42](https://github.com/nwejnkasdf/SKKU-insight/pull/42)**, **C-54 Fusion bridge_cso 영역 fresh Document fetch — UserProfile cron 안 apply_fusion_bridge_override 끝에 LLM web_search + DocumentTopic INSERT (D=bridge_cso 단일 매핑) + P1 dedup hint (직전 30일 fusion URL/title 회피) + 사용자 결정 6 + 자체 결정 6 + 빈틈 4 2026-05-24 [PR #43](https://github.com/nwejnkasdf/SKKU-insight/pull/43) + docs drift fix [PR #44](https://github.com/nwejnkasdf/SKKU-insight/pull/44)**, **C-55 클라이언트 IndexedDB offline queue — `client/src/lib/offlineQueue.ts` 신규 (~210 line) + InsightApi Proxy wrapper (mutation 4종 분기, network error 시 큐 enqueue + fake success / 4xx drop / 5xx enqueue+throw) + OfflineBanner (큐 internals 숨김, narrative 정책 정합) + 사용자 결정 4 + 자체 결정 5 2026-05-24 [PR #45](https://github.com/nwejnkasdf/SKKU-insight/pull/45)**, **C-56 leaf 활성 신호 ingest hook — `evaluate_rule_transitions` 의 production caller 가 `demotions` 만 apply 하던 결함 + `last_signal_active_day` 갱신 caller 부재 결함 (A7 P1-12 와 같은 패턴) fix. `interest/service.py:ingest_event_atomic` step 7.5 에 `_update_leaf_last_signal` + `_evaluate_leaf_promotion` hook 추가 — click/save/dwell_tick 시점 즉시 last_signal UPDATE + window 카운터 SQL + promotion (window_promotion/reactivation) only apply. 사용자 결정 3 (B/P/넓게) + 자체 결정 4. 회귀 가드 5. 스키마 변경 0 2026-05-24 [PR #46](https://github.com/nwejnkasdf/SKKU-insight/pull/46) + docs drift [PR #47](https://github.com/nwejnkasdf/SKKU-insight/pull/47)**, **C-57 split/retract LLM dispatch 본문화 — A7 본문의 stub (모두 동일 cso fallback) 을 LLM (high slot) 호출로 교체. `traversal/leaf_dispatch_llm.py` 신규 (~270 line, retract_reposition + split_dispatch 한국어 prompt) + `default.DefaultTraversalEngine._llm_retract_decisions` / `_llm_split_decisions` + `_fetch_leaf_summaries` helper. 실패 시 stub fallback 유지 (backward-compat 100%). 사용자 결정 6 (high/inline/stub-fallback/retract 2종/split 2종/같이) + 자체 결정 4. 단위 테스트 7 + 회귀 4. 스키마 변경 0 2026-05-24 [PR #48](https://github.com/nwejnkasdf/SKKU-insight/pull/48)**, **C-58 demo backfill 폐기 + pseudo Recommendation 자동 정리 — 실측 시연 (gywndrnjs123) 에서 "follow-up briefing" 가짜 자료 9건 노출 결함 fix. `_create_demo_backfill_candidates` 본문 → `return []` (~100 line → 12 line) + `_cleanup_pseudo_recommendations` 신규 (build_dashboard normal 진입 시 idempotent DELETE) + 회귀 가드 6 + 옛 Document 보존 (backward-compat) + 사용자 결정 1 (폐기 vs 대체) + 자체 결정 4. 스키마 변경 0 2026-05-25**) | (없음) | A2 + v13 라운드 + A4 + A6 + A7 + A8 + A8-v2 + P2 검토 + P1-12 fix + CSO 3.5 + WSL only + discovery 대규모 공사 |

**모든 P0 해소됨. P1-P2 활성 항목들은 default·stub 경로가 정해져 있어 모든 에이전트(A2-stub 포함)가 즉시 작업 시작 가능. v13 라운드 (C-33, 2026-05-11) 로 A4 collection 의 디자인이 LLM tool-use 모델로 pivot — P1-6/P2-3/P2-4 자연 무효. A4 코드 구현 + Codex 3-라운드 audit + 통합 시연 검증 (C-34/C-35/C-36, 2026-05-16~17) [PR #16](https://github.com/nwejnkasdf/SKKU-insight/pull/16) 완료. A6 본문 + Codex 2-라운드 audit + 통합 시연 검증 (C-37/C-38, 2026-05-17) [PR #18](https://github.com/nwejnkasdf/SKKU-insight/pull/18) 완료. A7 본문 (leaf-lifecycle + traversal, trace operation 4→5 + merge 신규) + Codex 3-라운드 audit (C-39, 2026-05-17) 완료. A8 본문 (recommendation engine — core/adjacent/discovery + cold-start orchestrator + 첫 trace 생성 hook A7 #6 plan TBD 완성) + R1 self-review fix (C-40, 2026-05-17) 완료. CodexOAuth 본문 + reasoning_effort fix (C-41, 2026-05-18) 완료. A8-v2 UserProfile + Discovery Fusion + Reincarnation Pivot + Codex R1 audit + R1 fix 7건 (C-42, 2026-05-19) 완료 [PR #25](https://github.com/nwejnkasdf/SKKU-insight/pull/25) merge `63f2cdde`. P2 백로그 그룹 A+B 검토 (C-43, 2026-05-19) — P2-10/16/19/21 close + CSO 3.4 → 3.4.1 전환 + --reset orphan 가드 + `seed-cso-cache` Makefile 타깃 [PR #27](https://github.com/nwejnkasdf/SKKU-insight/pull/27) merge `923063e`. P2 백로그 그룹 C 일부 (C-44, 2026-05-19) — P2-27 archived_at_active_day 분리 + P2-28 candidate_pool_ids 카테고리별 분리 close. A7 P1-12 fix (C-45, 2026-05-20) — extend/split caller 가 production code 어디서도 호출 안 되던 결함을 `daily_lifecycle_evaluation_job._evaluate_trace_expansion_for_user` 신규 helper 로 해결. alembic 0009 + `UserEvent.active_day_at_event` 컬럼 (extend window 를 active day delta 정합). 운영 도구 신규 (simulate_user_day.py + 3 Make target + manual-day-control.md). live 검증 (gywndrnjs123@naver.com, ad=93→94): traces 4→5, max_path_len 1→2, `extended=2 split=0 retracted=1 archived=4`. archive 는 `archive_if_eligible` 이름으로 이미 caller 존재 (P1-12 의 grep false negative — 별도 fix 불필요). [PR #30](https://github.com/nwejnkasdf/SKKU-insight/pull/30) merge `1861ed6`. **CSO 3.4.1 → 3.5 + git-tracked 전환 (C-46, 2026-05-24)** — `data/cso/CSO.3.5.csv` (25.7 MB, CC BY 4.0 attribution `NOTICE.md`) raw commit + `Makefile::seed-cso-cache` default FILE = `data/cso/CSO.3.5.csv` (FILE 생략 시 git-tracked 자동 사용, ~/Downloads 의존성 제거) + Settings/.env.example/docker-compose 의 `CSO_DOWNLOAD_URL` 도 3.5 로 갱신 (fallback) + cso_importer/mapping/import_cso 주석 7곳 + 13 docs 정합 (P1-5 close). 파서 변경 불필요 (3.4.1+ 와 csv-quoted N-Triples 형식 동일). 5 cluster seed 라벨 (Operating Systems / Automata Theory / Interactive Computer Graphics / Multimedia Systems / Scientific Computing) 은 3.4.1 호환 유지, 시연 시 cluster 부재 발견 시 재교체 reminder — **C-47 라운드 lifespan 검증으로 5 cluster 부재 0건 확인** (`clusters=12`). **Docker = WSL only 환경 규칙 (C-47, 2026-05-24)** — 본 프로젝트의 모든 `docker` / `docker compose` 호출은 WSL 안에서 실행 강제 (사용자 환경 = WSL native Docker Engine Community 29.x, Windows side `npipe:////./pipe/dockerDesktopLinuxEngine` 미사용). AGENTS.md "시연 30분 전 최종 체크" 박스 상단에 🐧 실행 환경 박스 + runbooks.md / docker-compose.md 상단 §실행 환경 + memory `docker_wsl_only.md` (feedback type) + MEMORY.md 인덱스. CSO 3.5 lifespan 검증 완료 (사용자 띄운 `gifted-vaughan-b92a01-api-1` 컨테이너): `nodes=14707 edges=44131 clusters=12 / elapsed_seconds=0.43 / provider=codex_oauth / system_config_loaded=true / event=lifespan startup ok` → C-46 라운드의 "5 cluster 미검증 reminder" 해소. **다음 진입 가능 모듈**: 시연 narrative 진행 (signup → onboarding → cold_start → dashboard 10 카드, A9 electron-client UI 까지), A9/A10 시연 통과 확인 (main 다수 보강 머지 완료, 사용자 검수 트리거 보류), Codex R2 재감사 (A8-v2 + C-45/C-46/C-47 회귀), 5 persona × 실 GPT-5.5 fusion 카드 시연, A11 (test-ci) 진입, A12 (demo-seed) 진입, P2-26 + P2-29 잔여 그룹 C 후속 세션.**

## 본 백로그의 출처

본 문서는 다음 위치의 마커를 한 번에 수집·정리한 결과다. 각 마커는 그대로 유지되며, 해결 시점에 해당 라인의 `<!-- TODO: -->` 또는 `# TODO:` 를 제거한다. 본 백로그 자체는 진척에 따라 갱신.

- `algorithms/cold-start.md` (2)
- `algorithms/clickbait-integration.md` (3, P0-1·P2-6 해소되어 마커 제거; vLLM 결정 추가)
- `algorithms/interest-bayesian.md` (1)
- `api/auth.md` (1)
- `data/cso-import.md` (1)
- `data/schema.md` (1)
- `data/seed-personas.md` (1)
- `data/sources-registry.md` (~22 verify URL + 2 보강)
- `security/password-policy.md` (1)
- `sdd/architecture.md` (1, P0-1 해소되어 마커 제거)
- `ux/wireframes.md` (정리 작업으로 본 백로그 P1-7로 이동)

마지막 정리: 2026-05-17 (**A8 recommendation engine 완전 완료** — 7 commit-stack + Codex 3 라운드 + 실 GPT-5.5 통합 시연 검증 (C-40)). c82ce83 PR-1 alembic 0006 + ORM 3 / 6476037 PR-2 본문 11 파일 / ce35d8e PR-3 tests 9 파일 / 15883d1 PR-4 R1 self-review fix / ebdd11f docs drift / 099f837 PR-5 R2 Codex 재감사 fix (Critical #2 UTC 경계 + Suggested #1 Lua atomic) / ee627a2 PR-6 R3 통합 시연 fix (GPT-5 temperature/max_tokens 분기). **시연 검증** (docker compose + 실 OpenAI chat/completions GPT-5.5, HTTP 200 61s): alembic 0006 functional index P2-23 통과 + 10 cards (실 논문/뉴스 추천) + 5/3/2 slot 분배 + Korean reason 23~31자 (≤80 검증) + NFR-04 score 미노출 + 2회차 cache hit + cold_start=true + sentinel `cold_start_pseudo` 활용. P2-23 backlog 해소. P2-22/24 운영 단계 후속. 이전: 2026-05-17 (A6 interest-bayesian 본문 + Codex round 1 fix 8건 (C-37) + round 2 재감사 fix 4건 (C-38) — [PR #18](https://github.com/nwejnkasdf/SKKU-insight/pull/18) 머지 commit `a0a3fbf`. 합계 12건 추가 해소. docker compose 격리 통합 시연으로 alembic 0001→0004 + 22 tables + system_config_loaded + signup/consent/JWT 흐름 + `/interest/state` 200 + NFR-04 마스킹 + `/events` idempotency 200/409 + `/events/batch` 207 정합 검증. PR #19 docs drift fix (`a2930cf`) 후속). 이전: 2026-05-17 (A4 v13 코드 구현 + Codex round 2 fix 15건 (C-34) + round 3 재감사 fix 7건 (C-35) + 통합 시연 발견 fix 4건 (C-36) — [PR #16](https://github.com/nwejnkasdf/SKKU-insight/pull/16) 머지. 합계 26건 추가 해소). 이전: 2026-05-11 (P0-1·P2-6 해소 + clickbait_module 코드 phase 완료로 P1-8·P2-7 해결 — 합계 4건 해소).
